# Debina Enterprise Rebase — Phase D

**Status:** `COMPLETE` (2026-07-23). **Baseline:** `676d09e19393830936f5bf419b40f4a5eaa2a4c3`. **Branch:** `rebase/enterprise-evolution`.

## Completed evidence

- The required baseline was verified. The user-owned `build/generated-spring-modulith/javadoc.json` remains unaltered at bootstrap SHA-256 `9c484e010bfa0a8719f78dd4ade744fe7e08a3a9fe7eaf0fb35ed1dd2ca0a015`.
- Repository inputs and ADR-N16's actual smoke capability inventory were inspected and recorded. The program records D/E/F on this long-lived branch while historical completed-phase branch references remain untouched.
- D0 now passes: Dagger CLI/Engine `v0.21.4`, Go `1.26.5`, Podman `5.8.4` rootful, socket access without `sudo`, privileged Engine PID limit `16384`, and a deterministic Alpine Dagger probe. The former environment blocker is superseded without Phase D changing host configuration.
- One CLI-generated Go SDK module now exists (`dagger.json` → `dagger/`), pins Engine `v0.21.4`, and exposes native `fast`.
- `dagger call fast` passed. It runs the authoritative governance runner (PASS with the established 296 traceability and 69 planning legacy warnings), non-mutating module self-verification, JDK 25 Maven compilation plus six selected architecture/source-boundary tests, and the pinned Node `24.18.0`/pnpm `10.33.0` codegen-drift, lint and typecheck leaf.
- D2A's historical checkpoint completed all socket-free native integration leaves through `dagger call integration`: frontend production build; the then-current 128 non-Testcontainers backend tests; ephemeral PostgreSQL 18 readiness; Flyway fresh migrate/validate; the approved V54 → V60 upgrade/validate; real-role RLS/grant probes; and a pinned `apache/kafka:4.1.1` non-production create/produce/consume probe. The final architecture replaces that selection with the durable 146-case `fast` tag. No host ports, volumes, runtime socket mounts, Docker, or host configuration changes were used.
- D2B supersedes the former socket-accessor interpretation. The v0.21.4 CLI accepted `/run/podman/podman.sock` as `--runtime-socket`, converted it to the generated `Address.socket: Socket!` argument, and `Container.WithUnixSocket` mounted it only into the disposable diagnostic and dedicated Maven/Testcontainers containers at `/var/run/docker.sock`. `_ping` returned `OK`. No socket copy, proxy, Docker compatibility socket, unrelated mount, or host change was used.
- The first Maven/Testcontainers attempt created Ryuk but could not reach its published `localhost` port. A socket-free Dagger probe resolved `host.containers.internal` to `10.88.0.1` and connected to the active Ryuk port; the dedicated Maven container consequently sets only `DOCKER_HOST=unix:///var/run/docker.sock` and the evidence-backed `TESTCONTAINERS_HOST_OVERRIDE=host.containers.internal`.
- The approved D2B portability correction is committed separately: `SettlementFinalityServiceTest` retrieves `finality_at timestamptz(3)` through typed JDBC as `OffsetDateTime`, converts it to `Instant`, and asserts exact equality with its fixed millisecond-precision authority instant. No timezone/session setting, text comparison, production source, migration or finality semantics changed. The explicit focused proof passed 5/0/0; cold `testcontainers-regression` passed 540/0/0/0 in Maven 4m29s. Its immediate repeat exited 0 through Dagger's observable result cache in about one second, with the required typed socket argument still explicit.
- D3 inventory audit initially found all six ADR-N16 journeys `CAPABILITY-BLOCKED` on the absent Playwright/runtime foundation and EPIC-24's general sequencing. The approved 2026-07-22 Phase D ADR-N16 exception supersedes that gate only for the six capped local smoke journeys. It does not complete or unblock EPIC-24 stories, the Control Room, reporting/SSE, general acceptance, axe-core, visual/cross-browser coverage, remote CI or runtime compose.
- D3A and all three D3B browser leaves are proven through isolated ephemeral service graphs. The durable `smoke-suite` classification runs the complete six-journey ADR-N16 cap sequentially.
- D4 assurance is proven for expected failure semantics, secret redaction, cache invalidation and service DNS. Unexpected failures remain fail-closed and emit a bounded redacted artifact.
- `Acceptance` is the sole automatic Dagger check. It runs `fast` and `integration` in parallel, then `smoke-suite`; `pipeline-assurance` remains independent. `PhaseD`, `AllSocketFree`, legacy `All` and `Smoke` are callable compatibility aliases only. `FullLocal` adds `backend-testcontainers` exactly once through the explicit typed socket.
- The final architecture proof passed on the pinned Dagger CLI/Engine `v0.21.4`: focused classifications, complete sequential smoke-suite, unfiltered `dagger check --lock=frozen`, independent assurance, log-health validation, compatibility aliases and typed-socket `full-local`. The topology and duplicate-name regressions fail closed if an automatic child or repeated classification is introduced.

## Historical D3A implementation evidence

- D3A Chromium login → BFF session → health was first proven through direct ephemeral Dagger services. The narrow command `dagger call smoke-login-session-health --progress=plain` ran one Chromium test successfully in 1m05.17s. The proof uses only `http://frontend:3000` and `http://keycloak:8080`, real application-triggered authorization-code/PKCE, the callback, host-only HttpOnly `sepa_session`, safe `/api/session` claims and the authenticated application shell. Its identity contract requires non-empty stable `sub` and role `payment_submitter`; `preferredUsername` is intentionally nullable because `sepa-web` defaults only to `basic` and `sepa-guc` rather than the absent `profile` scope, and the existing shell renders `unknown user`. No token, authorization code, secret or cookie value is exposed.
- D3A runtime evidence (2026-07-22): the first Keycloak graph stalled because `start-dev --import-realm` was modelled as a non-terminating `WithExec` before `AsService`. The pinned image's inherited 9000/tcp and 8443/tcp were also removed. Keycloak now uses `ContainerAsServiceOpts{Args: ["start-dev", "--import-realm"], UseEntrypoint: true}` and exposes only 8080/tcp. `dagger call smoke-keycloak-readiness stdout --progress=plain` passed in 29.28s. Backend initially failed because the fresh shared PostgreSQL service had no `sepa_app`; this was a Dagger graph-ordering defect. The post-Flyway `/tmp/d3a-flyway-complete` artifact now orders migrate+validate before backend startup. `dagger call smoke-backend-readiness stdout --progress=plain` passed in 44.39s (exit 0), including Kafka join, 8081 TCP readiness and `/actuator/health`. Frontend readiness was the next historical leaf and is proven below.
- D3A frontend readiness (2026-07-22): production `pnpm install --frozen-lockfile` and `pnpm run build` passed. The initial `pnpm run start -- --hostname` incorrectly forwarded the extra separator to Next. The service now uses direct `pnpm exec next start --hostname 0.0.0.0 --port 3000` through `AsService.Args`. `dagger call smoke-frontend-readiness stdout --progress=plain` passed in 1m06.85s (exit 0): 3000/tcp was healthy and the root route returned the intended `307 Location: /payments`.
- D3A local browser-origin exception (2026-07-22): the canonical tracked realm remains unchanged. Only the Dagger runtime may derive an ephemeral import artifact from it which appends exactly `http://frontend:3000/api/auth/callback` to `sepa-web` redirect URIs and `http://frontend:3000` to web origins, when absent. This supports the alias-visible local browser graph only; it does not alter production realm configuration, remove localhost entries, add wildcards, or change client/security semantics.
- D3A ephemeral realm-overlay assurance (2026-07-22): `dagger/pure/realm_overlay.go` is the sole deterministic transformer and fail-closed structural verifier. It compares parsed structures bidirectionally, preserves every realm/client property, client order and canonical URI/origin ordering, and permits only the exact approved append. Focused pure tests cover malformed shapes, duplicate/missing clients and values, unauthorized object/list changes, wildcards, deterministic output and structural idempotency. The finite in-graph `go run ./cmd/realm-overlay` helper writes one shared local-only directory containing the verified realm and `verified.marker`; the marker is written only after realm output succeeds. Keycloak imports that exact realm as `1000:1000`, mode `0640`, while the alias-bound readiness client reads and prints the matching safe marker before its discovery request. The final measured command `dagger call smoke-keycloak-readiness stdout --progress=plain` returned `exit=0` in 36.37s, with realm import, 8080 TCP readiness and discovery all passing. The parent Dagger span displayed its known teardown `ERROR` after `Container.stdout DONE`; this is recorded display behavior, not a functional failure.
No Phase E work is authorized. Wave 12 at `5ebebb0` remains untouched.
Completion of this local verification phase does not complete or unblock
EPIC-24 stories and must not be generalized into product acceptance. Backend
test changes are limited to durable classification metadata and its
completeness regression; no production payment code, migration, GraphQL schema,
runtime compose, realm source, workflow, remote CI or `act` was changed.
