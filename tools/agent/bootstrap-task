#!/usr/bin/env bash
# Atomically bootstrap work/ACTIVE.json + task dir + QUEUE NOW entry.
set -euo pipefail
export DEBINA_AGENT_WRAPPER=1
repo_root="$(git rev-parse --show-toplevel 2>/dev/null || pwd)"
cd "$repo_root"
exec python3 tools/agent/bootstrap_task.py "$@"
