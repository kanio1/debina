#!/usr/bin/env bash
# Controlled ACTIVE phase/profile/scope updates.
set -euo pipefail
export DEBINA_AGENT_WRAPPER=1
repo_root="$(git rev-parse --show-toplevel 2>/dev/null || pwd)"
cd "$repo_root"
exec python3 tools/agent/set_task_state.py "$@"
