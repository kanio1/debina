#!/usr/bin/env bash
# Apply human approval marker into ACTIVE approval_state (does not forge markers).
set -euo pipefail
export DEBINA_AGENT_WRAPPER=1
repo_root="$(git rev-parse --show-toplevel 2>/dev/null || pwd)"
cd "$repo_root"
exec python3 tools/agent/approve_task.py "$@"
