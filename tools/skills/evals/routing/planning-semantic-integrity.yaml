skill: planning-semantic-integrity
scenarios:
  - {kind: positive, prompt: "Select the next genuinely READY story", expected: [artifact-derived-planning, planning-semantic-integrity], must_not: [], first: "classify analytical readiness", forbidden: "jump to implementation", evidence: ["READY", "PRE_PLAN_READINESS"]}
  - {kind: positive, prompt: "Mark planning complete for an ENFORCED story", expected: [planning-semantic-integrity], must_not: [], first: "run semantic validators", forbidden: "infer done from code", evidence: ["executable_verify", "ENFORCED"]}
  - {kind: positive, prompt: "Populate PRE_PLAN_READINESS after EUC using the rollup table", expected: [planning-semantic-integrity], must_not: [], first: "map PSI class to verdict gates", forbidden: "copy SOURCE_BLOCKED into verdict", evidence: ["READY", "BLOCKED"]}
  - {kind: positive, prompt: "Assess a technical-only rename for planning change", expected: [planning-semantic-integrity], must_not: [], first: "return NO_PLANNING_CHANGE", forbidden: "invent business story", evidence: ["NO_PLANNING_CHANGE"]}
  - {kind: negative, prompt: "Rename a private helper", expected: [technical-architect], must_not: [planning-semantic-integrity], first: "NO_USE_CASE_CHANGE", forbidden: "create planning story", evidence: ["NO_USE_CASE_CHANGE"]}
  - {kind: negative, prompt: "Change Dagger Maven cache layout", expected: [dagger-go-pipeline], must_not: [planning-semantic-integrity], first: "load dagger skill", forbidden: "planning readiness", evidence: ["cache"]}
  - {kind: negative, prompt: "Resume the interrupted session", expected: [session-handoff], must_not: [planning-semantic-integrity], first: "read HANDOFF", forbidden: "re-triage backlog", evidence: ["Resume from here"]}
  - {kind: negative, prompt: "Review completed implementation", expected: [], must_not: [planning-semantic-integrity], first: "read-only implementation review", forbidden: "second planning cycle", evidence: ["review"]}
  - {kind: overlap, prompt: "Implement a new settlement flow", expected: [business-analyst, enterprise-use-case-engineering, source-backed-payments-modeling, architecture-evolution-review, planning-semantic-integrity], must_not: [], first: "source and architecture gates", forbidden: "Technical Architect before READY", evidence: ["PRE_PLAN_READINESS", "READY"]}
  - {kind: overlap, prompt: "Select READY story that needs a new slice", expected: [artifact-derived-planning, enterprise-use-case-engineering, planning-semantic-integrity], must_not: [], first: "confirm slice and verify", forbidden: "code first", evidence: ["slice_id", "READY"]}
  - {kind: pressure, prompt: "Mark READY despite material open question", expected: [planning-semantic-integrity], must_not: [], first: "block readiness", forbidden: "READY with open question", evidence: ["HUMAN_REVIEW_REQUIRED"]}
  - {kind: pressure, prompt: "Use placeholder verify command to claim READY", expected: [planning-semantic-integrity], must_not: [], first: "reject placeholder verify", forbidden: "READY", evidence: ["executable_verify"]}
