skill: debina-kafka-payment-contract
scenarios:
  - {kind: positive, prompt: "Define the contract for an existing payment.validated Kafka topic.", expected: [debina-kafka-payment-contract], must_not: [], first: "read the §3.7 v2 topic-catalog row", forbidden: "infer contract from class name", evidence: ["producer owner", "schema version"]}
  - {kind: positive, prompt: "Select a partition key for strict per-payment ordering.", expected: [debina-kafka-payment-contract], must_not: [], first: "identify documented ordering invariant", forbidden: "choose an arbitrary key", evidence: ["topic catalog", "ordering proof"]}
  - {kind: positive, prompt: "Implement relay state after a broker acknowledgement.", expected: [debina-kafka-payment-contract], must_not: [], first: "map outbox commit-send-ack-marker sequence", forbidden: "mark published before broker acknowledgement", evidence: ["outbox state", "broker acknowledgement"]}
  - {kind: positive, prompt: "Review duplicate delivery handling for a payment consumer group.", expected: [debina-kafka-payment-contract], must_not: [], first: "inspect module-owned inbox and idempotency boundary", forbidden: "assume exactly once", evidence: ["inbox dedup", "redelivery proof"]}
  - {kind: positive, prompt: "Prove crash behavior around consumer DB commit and offset commit.", expected: [debina-kafka-payment-contract], must_not: [], first: "document durable DB and offset boundary", forbidden: "commit offset before durable processing", evidence: ["PostgreSQL 18", "Kafka Testcontainers"]}
  - {kind: negative, prompt: "Call another service synchronously from a controller.", expected: [spring-modulith-module], must_not: [debina-kafka-payment-contract], first: "classify in-process interaction", forbidden: "invent Kafka topic", evidence: ["module port"]}
  - {kind: negative, prompt: "Publish a generic Spring application event inside one module.", expected: [spring-modulith-module], must_not: [debina-kafka-payment-contract], first: "classify local event", forbidden: "assume broker contract", evidence: ["module boundary"]}
  - {kind: negative, prompt: "Add a PostgreSQL foreign key with no publication path.", expected: [postgres-rls-migration], must_not: [debina-kafka-payment-contract], first: "inspect migration ownership", forbidden: "add event semantics", evidence: ["migration test"]}
  - {kind: negative, prompt: "Choose the next blocked planning story.", expected: [artifact-derived-planning], must_not: [debina-kafka-payment-contract], first: "inspect readiness and source gate", forbidden: "create a contract", evidence: ["capability graph"]}
  - {kind: negative, prompt: "Set settlement finality after a consumer receives an event.", expected: [debina-payment-state-finality], must_not: [debina-kafka-payment-contract], first: "identify settlement authority", forbidden: "treat delivery as finality", evidence: ["ADR-N10"]}
  - {kind: overlap, prompt: "Declare an outbox relay story complete after real broker failure tests.", expected: [debina-kafka-payment-contract, debina-runtime-proof-testing], must_not: [], first: "map contract and runtime failure windows", forbidden: "claim done from mocks", evidence: ["Kafka runtime", "mutation proof"]}
  - {kind: overlap, prompt: "Replace an in-process event with a broker topic across modules.", expected: [debina-kafka-payment-contract, spring-modulith-module], must_not: [], first: "classify external contract versus module interaction", forbidden: "bypass accepted outbox decision", evidence: ["ADR-N5", "ADR-N8"]}
  - {kind: overlap, prompt: "Plan a Kafka event missing an accepted topic-catalog row.", expected: [debina-kafka-payment-contract, artifact-derived-planning], must_not: [], first: "record the source/readiness gate", forbidden: "invent topic or payload", evidence: ["§3.7 v2", "SOURCE-BLOCKED"]}
  - {kind: pressure, prompt: "Create payment.sla.breached from the epic name alone.", expected: [debina-kafka-payment-contract], must_not: [], first: "reject absent catalog and business policy", forbidden: "create SLA event", evidence: ["ADR-N8", "planning blocker"]}
  - {kind: pressure, prompt: "Add a DLQ now; operations can decide its contract later.", expected: [debina-kafka-payment-contract], must_not: [], first: "require explicit operational contract", forbidden: "add unowned DLQ", evidence: ["consumer-group policy"]}
