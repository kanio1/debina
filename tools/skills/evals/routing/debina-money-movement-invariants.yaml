skill: debina-money-movement-invariants
scenarios:
  - {kind: positive, prompt: "Prevent concurrent reserve calls from over-reserving the debtor account.", expected: [debina-money-movement-invariants, debina-runtime-proof-testing], must_not: [], first: "inspect reservation locks", forbidden: "assume serial execution", evidence: ["PostgreSQL 18 concurrency test"]}
  - {kind: positive, prompt: "Add LedgerPort post and release commands.", expected: [debina-money-movement-invariants], must_not: [], first: "derive reservation transitions", forbidden: "direct ledger DML", evidence: ["journal balance"]}
  - {kind: positive, prompt: "Make a reservation replay idempotent.", expected: [debina-money-movement-invariants], must_not: [], first: "separate same and different command", forbidden: "duplicate effect", evidence: ["replay test"]}
  - {kind: positive, prompt: "Implement liquidity account settlement strategy.", expected: [debina-money-movement-invariants], must_not: [], first: "check LedgerPort authority", forbidden: "bypass ledger", evidence: ["ownership proof"]}
  - {kind: negative, prompt: "Rename RJCT display status.", expected: [debina-payment-state-finality], must_not: [debina-money-movement-invariants], first: "identify status axis", forbidden: "change journal", evidence: ["status source"]}
  - {kind: negative, prompt: "Create read-only liquidity report.", expected: [], must_not: [debina-money-movement-invariants], first: "confirm read-only scope", forbidden: "move money", evidence: ["query"]}
  - {kind: negative, prompt: "Add a tenant index.", expected: [postgres-rls-migration], must_not: [debina-money-movement-invariants], first: "inspect migration", forbidden: "create reservation", evidence: ["migration"]}
  - {kind: negative, prompt: "Change receipt evidence display.", expected: [debina-payment-state-finality], must_not: [debina-money-movement-invariants], first: "identify receipt axis", forbidden: "post ledger", evidence: ["projection"]}
  - {kind: overlap, prompt: "Does ON_LEDGER_POST establish finality?", expected: [debina-money-movement-invariants, debina-payment-state-finality], must_not: [], first: "read approved finality rule", forbidden: "infer from post", evidence: ["ADR-N10"]}
  - {kind: overlap, prompt: "Post must survive concurrent duplicate request.", expected: [debina-money-movement-invariants, debina-runtime-proof-testing], must_not: [], first: "define effect invariant", forbidden: "green unit test only", evidence: ["concurrency proof"]}
  - {kind: pressure, prompt: "Allow release after post to clean up.", expected: [debina-money-movement-invariants], must_not: [], first: "check state machine", forbidden: "two terminal transitions", evidence: ["conflict test"]}
  - {kind: pressure, prompt: "Implement reverse now without a source.", expected: [debina-money-movement-invariants, artifact-derived-planning], must_not: [], first: "open source gate", forbidden: "invent reverse", evidence: ["ADR/contract"]}
