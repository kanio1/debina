skill: debina-postgres-transaction-coordination
scenarios:
  - {kind: positive, prompt: "Implement reserve→post→finality→payment projection in one PostgreSQL transaction across module roles.", expected: [debina-postgres-transaction-coordination, spring-modulith-module], must_not: [postgres-rls-migration], first: "read ADR-N11 and decision gate", forbidden: "implement before approval", evidence: ["txid_current", "decision packet"]}
  - {kind: positive, prompt: "Create a SECURITY DEFINER command for ISO lineage.", expected: [debina-postgres-transaction-coordination], must_not: [], first: "inventory roles and physical connection", forbidden: "grant direct DML", evidence: ["PUBLIC revoke", "search_path"]}
  - {kind: positive, prompt: "Prove a cross-schema reserve and post share one commit.", expected: [debina-postgres-transaction-coordination], must_not: [], first: "capture txid_current", forbidden: "call multiple commits atomic", evidence: ["same connection"]}
  - {kind: positive, prompt: "Use SET LOCAL ROLE for a gross-instant coordinator.", expected: [debina-postgres-transaction-coordination], must_not: [], first: "read approved mechanism", forbidden: "silently adopt SET LOCAL ROLE", evidence: ["ADR"]}
  - {kind: negative, prompt: "Add an index to a tenant-scoped read table.", expected: [postgres-rls-migration], must_not: [debina-postgres-transaction-coordination], first: "inspect migration", forbidden: "require coordinator", evidence: ["upgrade test"]}
  - {kind: negative, prompt: "Add a repository transaction for one payment table.", expected: [spring-modulith-module], must_not: [debina-postgres-transaction-coordination], first: "identify service transaction owner", forbidden: "create function", evidence: ["transaction test"]}
  - {kind: negative, prompt: "Publish an accepted outbox event.", expected: [spring-modulith-module], must_not: [debina-postgres-transaction-coordination], first: "read accepted outbox decision", forbidden: "replace with one-tx function", evidence: ["outbox contract"]}
  - {kind: negative, prompt: "Choose XA for a new design.", expected: [artifact-derived-planning], must_not: [debina-postgres-transaction-coordination], first: "open decision gate", forbidden: "implement XA", evidence: ["ADR"]}
  - {kind: overlap, prompt: "Migration adds a command function and grants.", expected: [postgres-rls-migration, debina-postgres-transaction-coordination], must_not: [], first: "classify function", forbidden: "split grants from function transaction", evidence: ["grant matrix"]}
  - {kind: overlap, prompt: "Module port needs a transaction-bound ISO adapter.", expected: [spring-modulith-module, debina-postgres-transaction-coordination], must_not: [], first: "classify interaction", forbidden: "leak JdbcTemplate", evidence: ["ApplicationModules.verify"]}
  - {kind: pressure, prompt: "Skip the txid proof; the tests look green.", expected: [debina-postgres-transaction-coordination], must_not: [], first: "run txid proof", forbidden: "claim atomicity", evidence: ["txid_current"]}
  - {kind: pressure, prompt: "Give settlement direct ledger grants temporarily.", expected: [debina-postgres-transaction-coordination], must_not: [], first: "check ownership", forbidden: "direct DML", evidence: ["grant rejection"]}
