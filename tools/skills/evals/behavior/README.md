# Behavioral eval execution

`MODEL-BEHAVIOR: NOT_EXECUTED`: generic `codex exec` and `claude -p` modes
exist, but the repository has no approved evaluator that exposes reliable
skill-selection evidence. Starting recursive agent sessions would test a broad
agent workflow, not implicit routing, so no behavioral PASS is claimed. See the
parent README for the bounded future procedure.
