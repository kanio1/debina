# HANDOFF

## Zadanie

Sesja `EPIC-35-STORY-35.1-SETTLEMENT-STRATEGY-RESOLVER` (2026-07-17, nowa sesja Claude Code CLI, branch `main`, `EXPECTED_LOCAL_HEAD_PREFIX: c266d2d` — potwierdzony, worktree czysty na starcie poza pre-existing untracked `build/`). Cel: readiness audit + implementacja `EPIC-35` Story 35.1 (`SettlementStrategyResolver(settlement_basis, liquidity_mode)`, czysty domenowy resolver, zero DB/Kafka/Spring). `PUSH: FORBIDDEN`, `FETCH: FORBIDDEN`, `PULL: FORBIDDEN`, `FEATURE_BRANCH_CREATION: FORBIDDEN`, `DATABASE_MIGRATIONS: FORBIDDEN`, `ACT: ABSOLUTELY FORBIDDEN` — wszystkie honorowane. Nie rozpoczęto: Story 35.2 (ArchUnit), Story 35.3 (profile snapshot), żadnej konkretnej strategii (EPIC-36..41), `EPIC-32` (Ledger), `EPIC-43` Story 43.1.

## Zrobione

**Preflight:** `branch=main`, `HEAD=c266d2d` (dokładnie zgodny z oczekiwanym), lokalny `main` 2 commity przed `origin/main` (`152808c`) — `8097b1a`+`c266d2d`, niezmienione tą sesją, push nadal nie wykonany. Worktree czysty poza pre-existing untracked `build/` (obecny już w gitStatus na starcie sesji, nietknięty).

**Obowiązkowy odczyt:** `HANDOFF.md` (sesja Story 31.3A), `planning/README.md`, `planning/AGENTS.md`, `planning/epics/EPIC-35/EPIC-12/EPIC-32/EPIC-36..41/EPIC-43`, `sepa-nexus-message-flow-and-data-blueprint.md` §4.11 (w całości, source of the taxonomy) + §8 EPIC-SETTLE-1..8 + §3.10 (ownership doc), `backend/AGENTS.md`, istniejące konwencje (`signature` module jako wzorzec: `package-info.java`, `SigningException` styl), `ModularityTest.java`, `reference_data/V9__...sql` (scheme_profiles DDL).

**Readiness audit — full PASS** (9/9 warunków): EPIC-12 Story 12.1 done; verify command wykonywalny; frozen basis+mode invariant jednoznaczny (§4.11 linia 973, `[MVP-BLOCKER]`); resolver może być czysty; brak potrzeby DB/LedgerPort/profilu/konkretnej strategii/migracji. Audyt istniejącego kodu: `com.sepanexus.settlement` nie istniał — brak duplikacji.

**Source-backed taxonomy** (zapisana w scratchpad, `epic-35-1-readiness.md`): `SettlementBasis` (6 wartości), `LiquidityMode` (8 wartości), `SettlementStrategyKind` (6 wartości) — wszystkie 1:1 ze źródła §4.11. Minimalna MVP-matryca zaimplementowana to dokładnie 4 pary, które źródło jawnie wiąże adnotacjami parentetycznymi ("DCA_POOL (gross)", "ISOLATED_SUBACCOUNT (deferred/file)", "NONE_INTERNAL (internal book)"): `GROSS_INSTANT+DCA_POOL→GROSS_INSTANT`, `NET_DEFERRED+ISOLATED_SUBACCOUNT→NET_DEFERRED`, `ACH_FILE_BATCH+ISOLATED_SUBACCOUNT→FILE_BATCH`, `INTERNAL_BOOK+NONE_INTERNAL→INTERNAL_BOOK`. Świadomie NIE zaimplementowano (brak jednoznacznego źródłowego wiązania, fail-closed a nie zgadywanie): `BULK_CGS_LIKE+TECHNICAL_ACCOUNT_LIKE`, `SIMULATED_PREFUNDED_INSTANT+PREFUNDED_RESERVE` (oba `[P1]`), każda para z `CENTRAL_BANK_MONEY_LIKE`/`COMMERCIAL_BANK_MONEY_LIKE`/`LAB_SYNTHETIC` (źródło nazywa je "risk labels", nigdy nie wiąże ze strategią).

**Implementacja:** `com.sepanexus.settlement` (nowy top-level moduł Modulith, `allowedDependencies = {}`, brak schematu, brak Springa/DB/Kafka): `SettlementBasis`, `LiquidityMode`, `SettlementStrategyKind` (enumy), `SettlementStrategyResolver` (klasa `final`, `Map`-backed lookup po `record StrategyKey`), `UnsupportedSettlementStrategyCombinationException` (fail-closed, komunikat zawsze dokładna para, nigdy generyczny fallback).

**Test-first:** `SettlementStrategyResolverTest` (17 przypadków: 4 legalne, 6 nielegalne, 2 świadomie-niewspierane P1, 3 null-handling, determinizm, dokładna-para-w-komunikacie). Structural RED potwierdzony (`cannot find symbol` na wszystkich 4 typach domenowych, test napisany fizycznie przed jakąkolwiek klasą produkcyjną). Semantic RED potwierdzony po dodaniu kompilującego się, ale rzucającego `UnsupportedOperationException` szkieletu (`Tests run: 17, Failures: 12, Errors: 5`). GREEN po pełnej implementacji (`17/17 PASS`, potwierdzone dwukrotnie w pełnej regresji).

**Mutation-proof — wszystkie 3 poprawnie złapane, cofnięte:**
1. Resolver zignorował `liquidityMode` (klucz tylko po basis) → `Tests run: 17, Failures: 3, Errors: 3` → cofnięte.
2. Zła strategia dla `NET_DEFERRED+ISOLATED_SUBACCOUNT` → dokładnie `resolvesLegalCombination` FAIL (1) → cofnięte.
3. Default fallback zamiast throw → dokładnie 12 testów `rejects*` FAIL → cofnięte.
Po każdym cofnięciu: `17/17 PASS`, `git diff --check` czyste, zero pozostawionych markerów mutacji (sweep grep czysty).

**Audyty:** profile/CSM-name grep (`TIPS|RT1|STEP2|STET|KIR|ELIXIR|CSM|profileCode|profileName|schemeProfile`) w `com.sepanexus.settlement` → wyłącznie Javadoc wyjaśniający zakaz, zero logiki wykonawczej. Infrastructure grep (`JdbcTemplate|DataSource|EntityManager|Repository|SQL verbs|KafkaTemplate|@KafkaListener`) → czysty. Cross-module grep (`signature|paymentlifecycle|routing|egress|ledger`) → czysty. `ModularityTest`+`OwnershipArchRulesTest`+`PaymentNoGodModuleTest` → `9/9 PASS` (potwierdzone dwukrotnie).

**Pełna regresja — INFRASTRUCTURE BLOCKED (nie naprawiono, poza zakresem tej story):** uruchomiona dwukrotnie, wynik bit-identyczny obu razy: `251 tests, 0 failures, 15 errors, BUILD FAILURE`. Wszystkie 15 błędów to `ApplicationContext`/Kafka-connect-refused w `WalkingSkeletonIntegrationTest`/`JsonDirectIngestionTest`/`PaymentControllerTest`/`SecurityConfigTest` — żaden nie dotyka `com.sepanexus.settlement`. Potwierdzone `git log`: wszystkie 4 pliki ostatnio modyfikowane 2026-07-14/15, nietknięte tą sesją. Diff tej sesji to wyłącznie nowe pliki pod `com.sepanexus.settlement` (`allowedDependencies={}`, strukturalnie nie może dotrzeć do konfiguracji Kafka/Springa innego modułu). Poprzedni HANDOFF (Story 31.3A) raportował `234/234 PASS` bez żadnych błędów Kafka-connect — regresja środowiskowa pojawiła się między sesjami, nie w tym diffie. Nie próbowano naprawiać (poza zakresem czystej domenowej story). Targeted `verify:` i wszystkie testy architektoniczne są niezależnie zielone.

**Review:** self-review PASS — zgodność ze źródłem, `[FREEZE]` basis+mode nienaruszone, brak scope creep, granice modułów (`allowedDependencies={}`), brak ukrytego cross-schema access (moduł nie ma schematu), jakość testów (non-vacuous, mutation-proof), brak `act`, evidence kompletne.

**Planning:** Story 35.1 → `done` (evidence w epic file). `EPIC-35` → `in-progress`. Story 35.2 oznaczona jako analitycznie `READY` (czysty ArchUnit, ten sam kształt co `OwnershipArchRulesTest`). Story 35.3 oznaczona `[CAPABILITY-BLOCKED]` — `reference_data.scheme_profiles` (V9) nie ma kolumn `settlement_basis`/`liquidity_mode`/`finality_rule`, żadne źródło nie mapuje istniejącego `family` na nową 6/8-wartościową taksonomię — decision gate, nie zgadywanie. `planning/README.md` epic row zaktualizowany. `planning/capability-graph.json` (`EPIC-35.35.1` status `not-started→done`) i `.mmd` (label `E35` `not-started→in-progress`, dodany węzeł `S351 done`) zaktualizowane. `story-inventory.json` przegenerowany (`--write`, 76 epików/279 stories bez zmiany liczby). Wszystkie walidatory (`generate --check`, `validate-story-inventory`, `validate-capability-graph`, `validate-agent-instructions`, `validate-database-skills`, `validate-governance`, `git diff --check`, `json.tool` x2) → PASS.

**Commity (2, bez push):** `caee957 feat(settlement): add basis-mode strategy resolver` (7 plików, kod+test), `5d2bf65 chore(planning): mark Story 35.1 done, record 35.3 catalog gap` (5 plików planningu).

## Utknęliśmy na

Nic merytorycznie nie blokuje Story 35.1 — w pełni ukończona i zweryfikowana. Jedyny nierozwiązany punkt to środowiskowy `INFRASTRUCTURE BLOCKED` na poziomie pełnej regresji (15 błędów Kafka-connect w 4 niepowiązanych testach) — potwierdzony deterministyczny na 2 uruchomieniach, potwierdzony niezwiązany z tym diffem, nie naprawiony (poza zakresem).

## Plan na następny krok

1. **Zbadać `INFRASTRUCTURE BLOCKED`** (Kafka-connect-refused w `WalkingSkeletonIntegrationTest`/`JsonDirectIngestionTest`/`PaymentControllerTest`/`SecurityConfigTest`) na początku następnej sesji — sprawdzić `podman ps -a` (widoczne leftover `act-debug-*`/`act-backend-test-*` kontenery sprzed 2 dni, `infra_kafka_1` exited 11h temu) i czy któryś z nich koliduje z portem/siecią, której Testcontainers-Kafka tych 4 testów potrzebuje przy starcie kontekstu Springa. Nie naprawiać jako część Story 35.1-podobnej pracy — osobny task.
2. **Rekomendowana kolejność `READY`**: `EPIC-35/Story 35.2` (`NoProfileNameSwitchTest`, czysty ArchUnit, bez Testcontainers) → `EPIC-43/Story 43.1` (`outbound_messages`+`SKIP LOCKED` dispatcher) → `EPIC-32` (Ledger core, verify wymaga korekty z ręcznego `psql` na Maven+Testcontainers przed `done`) → `EPIC-25/Story 25.3B`.
3. **Story 35.3** pozostaje `[CAPABILITY-BLOCKED]` do czasu source-backed DDL rozszerzenia `scheme_profiles` (lub powiązanej tabeli) o `settlement_basis`/`liquidity_mode`/`finality_rule`, lub jawnej decyzji użytkownika/zespołu co do mapowania istniejącego `family`.
4. Push `main` — nadal `FORBIDDEN` w tej sesji, nadal nie wykonany. Lokalny `main` teraz `5d2bf65` (4 commity przed `origin/main` `152808c`).

## Pułapki, których nie wolno powtórzyć

- **Nie mylić "pełna regresja BUILD FAILURE" z "moja zmiana jest zła"** — zawsze najpierw sprawdź `git log -1 -- <failing-test-file>` na każdym niepowodzeniu niepowiązanym z twoim diffem; jeśli plik nie był tknięty tą sesją i moduł, który dodałeś, strukturalnie nie może go dotknąć (`allowedDependencies={}`), to jest to pre-existing/environmental, nie regresja. Uruchom regresję dwukrotnie, żeby odróżnić deterministyczny infra-blocker od flaki.
- **Taksonomia `(settlement_basis, liquidity_mode)`→strategia w źródle NIE jest pełnym iloczynem kartezjańskim** — §4.11 wiąże tylko 4 pary jawnie (przez adnotacje parentetyczne przy `LiquidityMode`), reszta (w tym obie pary P1) nie ma jawnego wiązania. Nie zgaduj brakujących par nawet jeśli "wydają się oczywiste" (np. `BULK_CGS_LIKE`+`TECHNICAL_ACCOUNT_LIKE` wygląda logicznie, ale źródło nigdy tego nie potwierdza) — fail-closed jest poprawnym zachowaniem, nie luką.
- **`reference_data.scheme_profiles` (V9) nie ma dziś kolumn `settlement_basis`/`liquidity_mode`/`finality_rule`** — tylko węższy `family text`. Każda przyszła story próbująca odczytać profil i rozwiązać strategię (Story 35.3, `SettlementOrchestrator`) najpierw potrzebuje albo DDL rozszerzenia, albo jawnej decyzji mapowania `family`→`basis`+`mode` — nie zakładaj, że `family` już to koduje.
- Pułapki z poprzednich sesji (niezmienione, patrz git historia tego pliku): broad epic-level `depends_on` może manufakturować cykle; JPA write-behind pod przełączoną rolą; `act`+Testcontainers = zawsze `Could not find a valid Docker environment`; ArchUnit domyślnie skanuje test-classes; `SECURITY DEFINER` wymaga jawnego `SET search_path`; nie ufaj własnej powłoce Bash jako dowodowi dostępu innego programu.
