# HANDOFF

## Zadanie

Sesja `KAFKA-BASELINE-RECOVERY-PLUS-EPIC-35-STORY-35.2` (2026-07-17, nowa sesja Claude Code CLI, branch `main`). `EXPECTED_LOCAL_HEAD_PREFIX: 02d4373` z promptu był **nieaktualny** — rzeczywisty HEAD na starcie: `d16812a` ("EPIC-35 Story 35.1 finished"), jeden commit dalej, wypchnięty przez użytkownika poza tą serią sesji (dodaje `build/generated-spring-modulith/javadoc.json` jako tracked; `main == origin/main` dokładnie). Rozbieżność opisana, nie cofnięto historii. Worktree czysty poza tym jednym trackowanym plikiem `build/generated-spring-modulith/javadoc.json`, który regeneruje się jako efekt uboczny `./mvnw test` (auto-generowany JSON, treść = ostatnia klasa z Javadoc przetworzona przez plugin Spring Modulith doc — nieistotna semantycznie, nietknięta celowo, patrz "Pułapki" niżej).

Dwa sekwencyjne cele: **Gate A** (odzyskanie zielonego pełnego baseline) → **Gate B** (`EPIC-35` Story 35.2, `NoProfileNameSwitchTest`). Zgodnie z jawną instrukcją promptu: jeśli Gate A nie zamknięty w pełni (dwa zielone przebiegi pełnej regresji), Gate B **nie może się rozpocząć**. `PUSH: FORBIDDEN`, `FETCH: FORBIDDEN`, `PULL: FORBIDDEN`, `ACT: ABSOLUTELY FORBIDDEN`, `DATABASE_MIGRATIONS: FORBIDDEN` — wszystkie honorowane, żadna migracja nie powstała.

## Zrobione

**Preflight:** rozbieżność HEAD opisana wyżej. `git log -15`, `git branch -vv/-r` potwierdzone. `HANDOFF.md` (sesja Story 35.1), `planning/epics/EPIC-35-settlement-strategy-resolver.md`, `planning/README.md`, `planning/capabilities.yaml`, ostatnie 4 commity (`git show --stat` każdego) przeczytane — zgodne z pamięcią poprzedniej sesji. `backend/pom.xml`, `application.yml`, wszystkie klasy Kafka test-support (`KafkaIntegrationSupport`, `IsoKafkaIntegrationSupport`, `JsonDirectIngestionTest`) przeczytane w całości.

### Gate A — pełna, poprawiona diagnoza (koryguje błędną diagnozę z poprzedniej sesji)

Poprzedni `HANDOFF.md` (sesja Story 35.1) błędnie zakwalifikował wszystkie 15 błędów jako "Kafka connection refused" na podstawie WARN-ów w konsoli. **To było niepoprawne** — pełny stack trace z `backend/target/surefire-reports/*.txt` (nie skrócone podsumowanie konsoli) ujawnił **trzy odrębne, niepowiązane przyczyny**:

| Test class | Errors | Root cause | Kafka jest przedmiotem? | Klasyfikacja |
|---|---:|---|---:|---|
| `WalkingSkeletonIntegrationTest` | 2 | `java.net.ConnectException` przy `accessToken():79` — realne żądanie HTTP do `localhost:8080` (prawdziwy Keycloak, świadomie, patrz Javadoc testu: "not a mock JWT"). `infra_keycloak_1` był zatrzymany. | TAK (poprawnie owinięty przez `KafkaIntegrationSupport`, Kafka nie jest tu winna) | Środowiskowy precondition, celowy design, nie kod |
| `JsonDirectIngestionTest` | 5 | `ContainerLaunchException`: `java.io.IOException: Przerwany potok` (broken pipe) na gnieździe Podman podczas `CREATE` kontenera `postgres:18`. Reprodukowane **3/3** razy (2x poprzednia sesja + 1x ta sesja), identyczny stack trace. Przechodzi 100% niezawodnie uruchomiony samodzielnie (5/5, potwierdzone dwukrotnie). | NIE (własny izolowany Postgres-only kontener, poprawnie napisany) | `INFRASTRUCTURE BLOCKED` — patrz niżej |
| `PaymentControllerTest` | 6 | `Unable to determine Dialect without JDBC metadata` — test nigdy nie miał `@DynamicPropertySource`, polegał domyślnie na `application.yml`'s `localhost:5432` (`infra_postgres_1`, zatrzymany). Realne, naprawialne naruszenie `infra/AGENTS.md`: "Automated backend integration tests should prefer isolated Testcontainers instances... Do not make tests depend on data left in infra_postgres_1." | NIE (`PaymentService` w pełni zamockowany) | Naprawiony |
| `SecurityConfigTest` | 2 | Identyczna konfiguracja Springa jak `PaymentControllerTest` (ten sam `@SpringBootTest @AutoConfigureMockMvc @MockitoBean(PaymentService)`) → współdzielą JEDEN klucz cache kontekstu Springa; `PaymentControllerTest` wywołuje realną próbę (pokazuje Hibernate error), `SecurityConfigTest` trafia na "ApplicationContext failure threshold exceeded" tego samego zepsutego kontekstu. | NIE | Naprawiony (ten sam fix co wyżej) |

**Naprawa (commit `e99793d`):** `PaymentControllerTest`/`SecurityConfigTest` — każdy dostał własny izolowany `PostgreSQLContainer` przez `@DynamicPropertySource`, wzorem `JsonDirectIngestionTest`/`KafkaIntegrationSupport`. `spring.flyway.enabled=false` w obu (potwierdzone: żadna z 4 klas ani `application.yml` nie ustawia `spring.jpa.hibernate.ddl-auto` nigdzie w repo — Hibernate potrzebuje tylko osiągalnego JDBC do wykrycia dialektu, nie zmigrowanego schematu). Mutation-proof: usunięcie `@DynamicPropertySource` z `PaymentControllerTest` odtworzyło dokładnie oryginalny błąd (`Tests run: 6, Errors: 6`), cofnięte, `6/6 PASS` ponownie. `git diff --check` czyste, zero markerów mutacji.

**`infra_keycloak_1` uruchomiony** (`podman start`, nie `compose up`, nic nowego nie utworzone, wolumeny nietknięte) — `WalkingSkeletonIntegrationTest` wymaga tego z definicji (jego własny Javadoc), nie jest to obejście testu. Potwierdzone działanie: `curl` GET do endpointu tokenu zwrócił `405` (poprawne — endpoint akceptuje tylko POST) od razu po starcie.

**`JsonDirectIngestionTest` — `INFRASTRUCTURE BLOCKED`, nie naprawiony:** zdiagnozowany dokładnie — `Caused by: java.lang.RuntimeException: java.io.IOException: Przerwany potok` w `ApacheDockerHttpClientImpl.execute` podczas tworzenia kontenera, po ~63 wcześniejszych udanych `postgres:18`/Kafka kontenerach stworzonych sekwencyjnie w tym samym długo działającym forku JVM. Wykluczone: błąd w logice cyklu życia testu (poprawny wzorzec `synchronized`+`initialized`, identyczny z działającymi klasami wspólnymi); `ulimit -n` (1048576, nie ogranicza); Ryuk (startuje raz, normalnie); szum połączeń Kafka (osobny kanał TCP, nie może zakłócać unix-socketu Podman). Pozostaje: rzeczywisty limit odporności/przepustowości gniazda rootless-Podman pod długotrwałym sekwencyjnym obciążeniem Testcontainers w jednej sesji JVM — poza kontrolą kodu aplikacji/testu. Nie próbowano "naprawić" przez retry/sleep/disable (zakazane) ani przez spekulacyjne strojenie środowiska bez dowodu skuteczności (poza autoryzowanym zakresem).

**Wynik Gate A: `PARTIAL` — 3/4 przyczyn naprawionych i zweryfikowanych (targeted + mutation-proof), 1/4 (`JsonDirectIngestionTest`) pozostaje `INFRASTRUCTURE BLOCKED`, w pełni zdiagnozowany, zreprodukowany 3/3 razy.** Pełna regresja po naprawie: `Tests run: 251, Failures: 0, Errors: 5, Skipped: 0, BUILD FAILURE` (log: `/tmp/backend-full-after-kafka-fix-1.log`) — wszystkie 5 błędów to wyłącznie `JsonDirectIngestionTest`. **Zgodnie z jawną instrukcją sesji (sekcja 13, "Gate A FAIL"): Story 35.2 NIE została rozpoczęta.**

### Gate B — nie rozpoczęty

Zgodnie z protokołem: Gate A nie osiągnął dwóch zielonych pełnych przebiegów, więc `EPIC-35` Story 35.2 (`NoProfileNameSwitchTest`) pozostaje `not-started`, bez żadnych zmian w plikach planningu. Wszystkie walidatory governance uruchomione mimo braku zmian planningu (kontrola higieny) — `5/5 PASS`, `git diff --check` czyste.

**Commity (1, bez push):** `e99793d test(kafka): isolate broker-dependent application contexts` (2 pliki testowe).

## Utknęliśmy na

`JsonDirectIngestionTest`'s `ContainerLaunchException`/broken-pipe podczas tworzenia kontenera `postgres:18`, głęboko w sekwencyjnym pełnym przebiegu (po ~63 wcześniejszych kontenerach). Zreprodukowany deterministycznie 3/3 razy z identycznym stack trace. Test przechodzi niezawodnie w izolacji. Root cause: infrastrukturalny limit gniazda rootless-Podman pod długotrwałym obciążeniem, nie kod.

## Plan na następny krok

1. **Zbadać `INFRASTRUCTURE BLOCKED` (`JsonDirectIngestionTest`)** dalej — kandydaci do zbadania w kolejnej sesji: (a) czy istnieje bezpieczne, dobrze udokumentowane strojenie klienta `docker-java`/Testcontainers dla rootless Podman (np. connection-pool timeout) bez ryzyka efektów ubocznych na inne testy; (b) czy warto zredukować całkowitą liczbę oddzielnych kontenerów Postgres tworzonych w pełnym przebiegu (obecnie: `KafkaIntegrationSupport`, `IsoKafkaIntegrationSupport`, `JsonDirectIngestionTest`, teraz +`PaymentControllerTest`+`SecurityConfigTest` — 5 niezależnych miejsc tworzących własne kontenery) przez współdzielenie — **ale to wymaga starannej analizy ryzyka cross-class state leakage, nie robić pochopnie**; (c) zbadać `podman system df`/logi demona Podman podczas przebiegu, żeby złapać dokładny moment/przyczynę zerwania potoku.
2. **Gdy Gate A osiągnie dwa pełne zielone przebiegi**: rozpocznij `EPIC-35` Story 35.2 (`NoProfileNameSwitchTest`) dokładnie wg specyfikacji tego promptu — ArchUnit na granicy `SettlementStrategyResolver.resolve(SettlementBasis, LiquidityMode)`, fixtures `backend/src/test/java/com/sepanexus/architecturefixtures/settlementprofile/` (forbidden: `String profileName` selector; allowed: typed pair), mutation-proof 3x.
3. Push `main` — nadal `FORBIDDEN`, nadal nie wykonany. Lokalny `main` teraz `e99793d`.
4. `build/generated-spring-modulith/javadoc.json` pozostaje zmodyfikowany, niezacommitowany (regeneruje się przy każdym `./mvnw test`, treść niedeterministyczna/nieistotna — patrz "Pułapki").

## Pułapki, których nie wolno powtórzyć

- **Zawsze czytaj pełny stack trace z `backend/target/surefire-reports/*.txt`, nigdy nie diagnozuj wyłącznie ze skróconego podsumowania konsoli.** Poprzednia sesja błędnie skwalifikowała WSZYSTKIE 15 błędów jako "Kafka connection refused" na podstawie WARN-ów w konsoli (tło niezwiązanego, wciąż żywego konsumenta Kafka z INNEGO cache'owanego kontekstu) — rzeczywiste przyczyny to trzy zupełnie różne rzeczy (brakujący Keycloak, brakujący Postgres, transient Podman broken-pipe). Ten błąd kosztowałby całą sesję, gdyby nie zweryfikowano surowego stack trace'u.
- **`@SpringBootTest`-based testy, które mockują CAŁĄ warstwę serwisową, nadal potrzebują osiągalnej bazy danych** — Hibernate/JPA auto-config próbuje wykryć dialekt przy starcie kontekstu niezależnie od tego, czy jakikolwiek test faktycznie odpytuje bazę. Brak `@DynamicPropertySource` w pełnym `@SpringBootTest` = cichy fallback na `application.yml`'s domyślny `localhost:5432`, czyli na `infra_postgres_1` — dokładnie to, czego `infra/AGENTS.md` zakazuje jako "automatyczny fixture".
- **`build/generated-spring-modulith/javadoc.json` regeneruje się przy każdym `./mvnw test`** (jakiś plugin Spring Modulith doc bindowany do fazy `test`) i jego treść to po prostu "ostatnia klasa z Javadoc, jaką plugin przetworzył" — niedeterministyczne w zależności od kolejności/zakresu uruchomionych testów, semantycznie nieistotne. Nie commituj go rytualnie przy każdej sesji; nie traktuj jego zmiany jako sygnału czegokolwiek.
- **Testcontainers + rootless Podman + długi pojedynczy fork JVM (~250 testów, ~8-10 min) może hit'ować `broken pipe` na gnieździe Podman po dziesiątkach sekwencyjnych tworzeń kontenerów** — nie jest to błąd w kodzie testu, jeśli test przechodzi niezawodnie w izolacji. Nie próbuj "naprawić" przez retry/sleep — to jest zakazane i tak czy inaczej nie adresuje przyczyny (Testcontainers już ma wbudowany retry, który się wyczerpuje).
- Pułapki z poprzednich sesji (niezmienione): broad epic-level `depends_on` może manufakturować cykle; JPA write-behind pod przełączoną rolą; `act`+Testcontainers = zawsze `Could not find a valid Docker environment`; ArchUnit domyślnie skanuje test-classes; `SECURITY DEFINER` wymaga jawnego `SET search_path`; taksonomia `(settlement_basis, liquidity_mode)`→strategia w źródle NIE jest pełnym iloczynem kartezjańskim (§4.11 wiąże tylko 4 pary jawnie); `reference_data.scheme_profiles` nie ma dziś kolumn `settlement_basis`/`liquidity_mode`/`finality_rule`.
