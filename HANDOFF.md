# HANDOFF

## Zadanie

Sesja `EPIC-27-STORY-27.2 + CORRELATION-READINESS + PLANNING-CORRECTION + TESTCONTAINERS-FIRST` (2026-07-16, nowa, czysta sesja Claude Code CLI, `EXPECTED_BASE_COMMIT: de9cf40` — potwierdzony, worktree czysty na starcie). Cel: dostarczyć źródłowo poprawną, tenant-safe i niezależnie testowalną korelację pacs.002 (`EPIC-27` Story 27.2), poprzedzoną pełnym audytem gotowości (Część C promptu sesji) i korektą planningu przed kodem. `PRODUCTION_CODE_CHANGES: ALLOWED ONLY FOR EPIC-27 STORY 27.2 AFTER READINESS GATE`, `DATABASE_MIGRATIONS: ALLOWED ONLY FOR ISO CORRELATION CAPABILITY`, `PUSH: FORBIDDEN` — obie zasady honorowane. Nie rozpoczęto: Story 27.3/27.4/27.5, EPIC-20 Story 20.3 implementation, GraphQL, SECURITY DEFINER.

## Zrobione

**Readiness audit (Część C)** — pełna macierz w `/tmp/epic-27-2-readiness.md` (scratch, niezakomitowana):
- 9-krokowa polityka blueprintu podzielona poprawnie: kroki 1–7 → Story 27.2 (teraz 27.2A/B/C), krok 8 (`IGNORED_DUPLICATE`) → Story 27.3, krok 9 (late/weaker → FSM) → Story 27.3 + `EPIC-20` Story 20.3.
- **Krok 5 (fallback `EndToEndId+amount+currency+participant+time-window`) świadomie NIE zaimplementowany** — `iso.payment_iso_identifiers`/`iso.iso_messages` nie mają amount/currency/participant, źródło nie przypisuje tych danych do schematu `iso`, `iso-adapter` nie wolno czytać `payment.*` bezpośrednio. Zapisane jako `[OPEN-QUESTION]` w `planning/epics/EPIC-27-iso-correlation-engine.md` i `planning/README.md` (punkt 17).
- Tenant-scoping: `iso.iso_messages`/`iso.payment_iso_identifiers` nie miały `tenant_id` (mimo że źródłowy DDL §4.3c go definiuje) — rozwiązane migracją V21 (patrz niżej), bez czytania `payment.*` z `iso-adapter`.
- Score: źródło nie definiuje formuły/wartości liczbowych → `score` zawsze `NULL`; `matched_by` to kontrolowany enum `CorrelationMatchStrategy`, nigdy wolny tekst.
- Event/outbox: `iso.outbox_events` nie istniało (tylko `payment.outbox_events`) — dodane tym samym per-schema wzorcem (§4.4 wprost wymienia `iso.outbox_events` w przykładowej liście).

**Planning correction (Część D) — commit `ca0b0b0`:**
- `EPIC-27` Story 27.2 rozdzielona na **27.2A** (input model), **27.2B** (ordered policy, steps 1–4/6/7), **27.2C** (persistence + event) — ten sam wzorzec co `EPIC-10` Story 10.1→10.1A-E.
- `EPIC-20` Story 20.3 i `EPIC-65` Story 65.4: `depends_on` przekierowany z `EPIC-27/Story 27.2` na `EPIC-27/Story 27.2C` (konkretna zdolność, nie cały story).
- `tools/agent-config/generate-story-inventory.py` (nowy, stdlib-only, deterministyczny) zastępuje poprzedni jednorazowy skrypt spoza repo; `--check`/`--write`; wpięty w `validate-governance.sh` i `.github/workflows/planning-governance.yml`. **Ograniczenie udokumentowane w samym skrypcie**: `capability_analysis` dla 29 rekordów curated w poprzedniej sesji ("dependency-narrowing session") nie jest mechanicznie wyprowadzalne z tekstu pliku epika w sposób generyczny (marker occurs unevenly across files) — generator celowo cofa te rekordy do `NOT_DEEP_DIVED` zamiast zgadywać; jedyna mechanicznie wyprowadzalna reguła to `[DONE <data>]` per-story marker. `planning/story-inventory.json` **przegenerowany** tym narzędziem (277→279 rekordów po dodaniu 27.2A/27.2B/27.2C, usunięciu 27.2) — naprawił też 3 realne artefakty starego, niezakomitowanego skryptu (urwane fragmenty z zagnieżdżonych backticków w `verify:` dla EPIC-08/EPIC-11/EPIC-23).

**Implementation (Część F/G) — commits `82b3730` (27.2A/27.2B) i kolejny (27.2C, patrz niżej):**
- **27.2A**: `Pacs002CorrelationInput` (orgnlMsgId/orgnlTxId/orgnlInstrId/orgnlEndToEndId/orgnlUetr) + nowa metoda `Pacs002IdentifierExtractor.extractCorrelationInputs()` (addytywna — Story 27.1's `extract()`/`Pacs002OriginalIdentifiers` nietknięte, 11/11 nadal PASS). 10 testów, mutation-proof (zamiana tagu `OrgnlTxId` → 3 testy poprawnie zawiodły → cofnięte).
- **27.2B**: `Pacs002CorrelationPolicy` nad portem `CorrelationCandidateLookup`, ordered strategies 1→4, `AMBIGUOUS` natychmiast przy >1 kandydacie (bez fallbacku do słabszej strategii), `ORPHANED` po wyczerpaniu 4 strategii. 9 testów, mutation-proof dwukrotnie (branch AMBIGUOUS, early-return step 4) — oba razy mutacja poprawnie złapana i cofnięta.
- **27.2C**: migracja `V21__iso_message_correlation.sql` — `iso.iso_message_correlation` (append-only, DDL §4.3c), `iso.iso_messages.tenant_id` (backfill z `ingress.raw_inbound_messages` przez istniejący `raw_message_id` FK; **celowo NIE `NOT NULL`** — legacy V17 backfill rows nie mają `raw_message_id`, zostają `NULL`, nigdy nie fabrykowane), `iso.payment_iso_identifiers.tx_id` (kompletuje źródłowy DDL, zawsze `NULL` dziś — brak kanału `pacs.008`), `iso.outbox_events`. Produkcyjne klasy: `JdbcCorrelationCandidateLookup` (tenant-scoped, wyłącznie wewnątrz schematu `iso`), `IsoMessageCorrelationRecorder`, `IsoOutboxRecorder`/`IsoOutboxDispatcher`/`IsoCorrelationTopicConfig` (topic `iso.message.correlated`), `Pacs002CorrelationService` (jedna `@Transactional` jednostka). `JsonDirectLineageRecorder`/`Pain001LineageRecorder` zaktualizowane, by zapisywać `tenant_id` na każdym nowym wpisie (i ich wywołujący: `PaymentService`/`Pain001PersistenceService`). 7 testów Testcontainers (MATCHED/AMBIGUOUS/ORPHANED, tenant scoping, izolacja kolumn per-strategia, append-only grants, foreign-role rejection) + 1 dedykowany test upgrade-path (migracja do V20, seed legacy danych włącznie z wierszem bez `raw_message_id`, migracja do V21, potwierdzenie backfill + że legacy wiersz zostaje `NULL`). Mutation-proof: `IsoMessageCorrelationRecorder` wymuszony na zawsze `ORPHANED` → 2 testy poprawnie zawiodły → cofnięte.
- Pełna regresja backendu: **`205/205 PASS`**, `BUILD SUCCESS`.

**Database review** — `PASS`, zero blocking findings. Trzy non-blocking: (1) `iso.iso_messages`/`iso.payment_iso_identifiers` nadal bez RLS (ownership-grant model, zgodnie z istniejącą konwencją całego schematu `iso.*` — nie regresja, warto zrewidować po `EPIC-10` gdy `iso-adapter` dostanie własną rolę DB); (2) obserwowany przejściowy WARN log "permission denied for schema iso" podczas pełnej regresji, w kontekście Spring `SetLocalRoleSqlProofTest` — nowy `@Scheduled` `IsoOutboxDispatcher` ściga się z rolami przełączanymi przez ten proof test na współdzielonym connection poolu, złapane/zalogowane/połknięte (identyczny wzorzec odporności jak istniejący `payment` `OutboxDispatcher`), zero faktycznych niepowodzeń testów; (3) brak dedykowanego foreign-role-rejection testu specyficznie dla `iso.outbox_events` (pokryty pośrednio przez identyczny, już-dwukrotnie-udowodniony mechanizm grantów `ALTER DEFAULT PRIVILEGES` z V11).

## Utknęliśmy na

Nic nie blokuje merytorycznie. Story 27.2 (steps 1-7, jako 27.2A/27.2B/27.2C) w pełni ukończona i zweryfikowana. Krok 5 fallback pozostaje `[OPEN-QUESTION]` — patrz wyżej i `planning/README.md` punkt 17.

## Plan na następny krok

`EPIC-27` Story 27.3 (duplicate → `IGNORED_DUPLICATE`, late/weaker → payment-lifecycle FSM policy) jest kolejnym, celowo nierozpoczętym w tej sesji vertical slice — jego `depends_on` już poprawnie wskazuje `Story 27.2C` (done). Przed startem: aktywować te same skille (`sepa-nexus-flyway-safe-change`, `sepa-nexus-payments-data-integrity`, `sepa-nexus-database-testing`, `sepa-nexus-database-review`). Nie łączyć z `EPIC-20` Story 20.3 implementation (osobna, dalsza capability — MATCHED-result consumption do FSM, wciąż `blocked`, teraz na poprawnym, węższym dependency).

Alternatywnie, do rozstrzygnięcia przez użytkownika (nie tego session): krok 5 fallback `[OPEN-QUESTION]` (patrz wyżej) — wymaga albo nowego source-backed read portu do `payment-lifecycle` ujawniającego amount/currency/participant, albo jawnej decyzji o właścicielu tych danych. `EPIC-10`'s `SECURITY DEFINER` decision gate nadal `PROPOSED — REQUIRES USER ACCEPTANCE`, niezmieniony przez tę sesję.

## Pułapki, których nie wolno powtórzyć

- **`iso.iso_messages`/`iso.payment_iso_identifiers` nie miały `tenant_id`, mimo że źródłowy DDL (§4.3c) go definiuje** — V11 świadomie skróciła "minimal slice" bez tej kolumny. Zanim zaimplementujesz cokolwiek tenant-scoped w schemacie `iso`, sprawdź RZECZYWISTE kolumny migracji, nie tylko DDL-sketch w blueprincie — mogą się rozjeżdżać (i tu się rozjechały).
- **Legacy backfill rows (V17) nie mają `raw_message_id`** — każda przyszła migracja próbująca backfillować coś przez `raw_message_id → ingress.raw_inbound_messages` musi liczyć się z NULL-ami dla tych wierszy; nie fabrykuj wartości, zostaw `NULL` i udokumentuj dlaczego.
- **Dodanie nowego `@Scheduled` bean (np. drugi `OutboxDispatcher`) w tym samym Spring Modulith deployable powoduje, że poller odpala się w KAŻDYM pełnokontekstowym teście integracyjnym** — w tej sesji spowodowało to nieszkodliwy, ale widoczny WARN log podczas `EPIC-10`'s `SetLocalRoleSqlProofTest` (connection-pool race z rolą przełączaną przez SET LOCAL ROLE). Nie jest to awaria testu (złapane/zalogowane), ale dobrze wiedzieć, skąd bierze się log przy debugowaniu przyszłej sesji.
- **`story-inventory.json`'s `capability_analysis` "DEEP_DIVED: ... dependency-narrowing session" adnotacje z poprzedniej sesji były curated przez człowieka, nie mechanicznie derywowalne z tekstu pliku epika w generyczny sposób** — próba odtworzenia ich 1:1 przez prosty regex na całym pliku daje fałszywe pozytywy (epików, które faktycznie NIE były częścią tamtej sesji, ale przypadkiem zawierają podobny tekst gdzie indziej w pliku, np. inny `[CORRECTED ...]`/`[SPLIT ...]` tag z innej daty/sesji). Generator świadomie NIE odtwarza tych konkretnych 29 adnotacji (cofa je do `NOT_DEEP_DIVED`) zamiast zgadywać — to udokumentowane, świadome ograniczenie, nie błąd.
- Pułapki z poprzednich sesji (niezmienione, wciąż aktualne): broad epic-level `depends_on` może manufakturować cykle; epik "not done" może kryć gotową sub-capability; `blocked` story może kryć już-`done` pracę; JPA write-behind pod przełączoną rolą; `act`+Testcontainers = zawsze `Could not find a valid Docker environment`; ArchUnit domyślnie skanuje test-classes; `SECURITY DEFINER` wymaga jawnego `SET search_path`; `set_config(..., true)` wraca do `''` po commit i po rollback.
