# HANDOFF

## Zadanie

Sesja `EPIC-31-STORY-31.3A-STANDALONE-SIGNING` (2026-07-16, nowa sesja Claude Code CLI, branch `main`, `EXPECTED_LOCAL_HEAD_PREFIX: 152808c` — potwierdzony, worktree czysty na starcie). Cel: readiness audit + implementacja `EPIC-31` Story 31.3A (standalone Ed25519 signing capability, sign→verify round-trip, bez zależności od `egress`). `PRODUCTION_CODE_CHANGES: ALLOWED ONLY FOR EPIC-31 Story 31.3A`, `DATABASE MIGRATIONS: FORBIDDEN`, `PUSH: FORBIDDEN`, `REMOTE_BRANCH_DELETION: FORBIDDEN` — wszystkie honorowane. Nie rozpoczęto: `EPIC-43` Story 43.1/43.2, egress renderer, transport, real HSM/CA/OCSP, key rotation UI, multiple algorithms, `EPIC-27` Story 27.3/27.5, `EPIC-20` Story 20.3, GraphQL, `SECURITY DEFINER`.

## Zrobione

**Preflight:** `branch=main`, `HEAD=152808c` (dokładnie zgodny z oczekiwanym), worktree czysty, jedyna lokalna gałąź `main`. **Rozbieżność względem założeń promptu**: prompt zakładał "LOCAL MAIN CONTAINS UNPUSHED COMMITS", ale `git rev-parse origin/main` lokalnie już wskazywał na `152808c` (identyczne z `main`, `0 0` w `rev-list --left-right --count`) — nie wykonałem `git fetch` (zabronione), więc nie mogłem zweryfikować, czy to jest realny stan zdalnego repo czy tylko przestarzały lokalny cache tej referencji. Nie potraktowałem tego jako podstawy do jakiejkolwiek zmiany zachowania — `PUSH: FORBIDDEN` i tak obowiązywało niezależnie.

**Obowiązkowy odczyt:** `HANDOFF.md` (sesja main-consolidation), `EPIC-31-signature-module.md`, `EPIC-43-egress-rail-outbound-dispatch.md`, `sepa-nexus-signature-module-blueprint.md`, `sepa-nexus-full-blueprint-review-and-task-plan.md` §12.1, wszystkie istniejące klasy `com.sepanexus.signature`/`.internal`, wszystkie istniejące testy signature/ingress, migracje `V13`/`V14`.

**Readiness audit (Część 5-8 promptu) — kluczowe ustalenie**: źródłowy blueprint (linie 60-61) już definiował DWA odrębne porty — `SignatureVerificationPort`/`SignatureSigningPort` — podczas gdy kod miał jeden połączony `SignaturePort` (`sign()` rzucał `UnsupportedOperationException`). Jedynym realnym callerem `SignaturePort` był `SignedChannelIngestionPipeline` (tylko `.verify()`) — potwierdzone `grep`-em, zero wywołań `.sign()` gdziekolwiek — mały, lokalny refaktor bez potrzeby warstwy kompatybilności.
- **`signingKeyRef` semantyka**: brak konfliktu ze źródłem/istniejącymi danymi testowymi (`grep` po `privateMaterialRef`/`private_material_ref` — istniejące testy używają wyłącznie nieprzezroczystych placeholderów typu `"synthetic-lab-ref"`/`"ref-active"`, nigdy realnego formatu resolvera). Przyjęta konwencja (Część 6 promptu, "dozwolona decyzja minimalna"): `signingKeyRef` = tekstowa reprezentacja `UUID` wiersza `signature.signature_keys.id`.
- **Prywatny materiał**: konwencja `inline-pkcs8:<Base64 PKCS#8>` (Część 7 promptu) — brak konfliktu, żaden istniejący seed/test jej nie kontredykt.
- **Migracja**: niepotrzebna — `V13`/`V14` już miały wszystkie kolumny (`message_signatures.outbound_artifact_id`/`direction`, `signature_keys.private_material_ref`/`purpose`/`status`/`valid_from`/`valid_to`).
- Wszystkie punkty readiness gate (Część 8): **PASS**.

**Port split (Część 5, 9):** `SignaturePort` usunięty. Nowe: `SignatureVerificationPort` (`verify` only), `SignatureSigningPort` (`sign` only, publiczny port pozostaje minimalny — bez `Clock`/`DataSource`/typów infrastrukturalnych). `Ed25519SignatureVerifier` implementuje teraz wyłącznie `SignatureVerificationPort` (metoda `sign()` usunięta). `SignedChannelIngestionPipeline` (jedyny caller) zaktualizowany. Javadoc-referencje (`DetachedSignature`, `Verdict`, `SignatureVerificationRequest`, `package-info.java`, `PaymentController`, testy) zaktualizowane. `signature`'s `package-info.java` rozszerzony o `allowedDependencies = {"shared"}` (potrzebne dla `ClockPort` w nowym signerze — mirror konwencji już istniejącej w `modules`' `package-info.java`). Regresja po refaktorze: `28/28 PASS` (istniejące testy signature/ingress + `ModularityTest`) — zero zmiany zachowania Story 31.2.

**Implementacja signing (Część 10-12):** `Ed25519SignatureSigner` (`com.sepanexus.signature.internal`, implementuje `SignatureSigningPort`), `SigningKeyLookup` (nowy — odczyt `signature.signature_keys` po dokładnym `id`, inny kształt niż publiczny `KeyRegistryPort.lookup(participantId, purpose, asOf)`), `SigningKeyRecord` (internal-only, niesie `privateMaterialRef` — nigdy nie wystawiany publicznie), `PrivateKeyMaterialResolver` (`inline-pkcs8:` format, JCA `KeyFactory.getInstance("Ed25519")` + `PKCS8EncodedKeySpec`), `SigningException` (publiczny, 9 kontrolowanych reason code: `INVALID_ARTIFACT_BYTES`, `INVALID_SIGNING_KEY_REF`, `SIGNING_KEY_NOT_FOUND`, `SIGNING_KEY_NOT_ACTIVE`, `SIGNING_KEY_WRONG_PURPOSE`, `UNSUPPORTED_SIGNING_ALGORITHM`, `PRIVATE_KEY_MATERIAL_UNAVAILABLE`, `INVALID_PRIVATE_KEY_MATERIAL`, `SIGNATURE_PERSISTENCE_FAILED`; wiadomość wyjątku zawsze = dokładnie reason code, nigdy prywatny materiał). `sign()`: waliduje input → `SigningKeyLookup.findById` → purpose/status/validity/algo/private-material checks → real JCA `Signature.getInstance("Ed25519")` na dokładnych `artifactBytes` → jeden `INSERT` do `signature.message_signatures` (`direction=OUTBOUND`, `raw_message_id`/`outbound_artifact_id` oba `NULL` — brak inbound message i brak egress artifact w tej story; prawdziwe powiązanie to `EPIC-43` Story 43.2) → zwraca `DetachedSignature`. Insert-failure rzuca wyjątek PRZED zwróceniem podpisu.
- **Decyzja (pusty artefakt)**: `artifactBytes=null` → odrzucony (`INVALID_ARTIFACT_BYTES`); `artifactBytes` puste (zero-length, nie-`null`) → **dozwolone** — nie wymyślono zakazu, którego nie ma ani w źródle, ani w JCA.

**Test-first (Część 13-14):** RED potwierdzony fizycznym odłożeniem na bok 5 nowych plików produkcyjnych (`SigningException`, `SigningKeyRecord`, `SigningKeyLookup`, `PrivateKeyMaterialResolver`, `Ed25519SignatureSigner`) do scratchpad, uruchomieniem `-Dtest=*SigningRoundTripTest*` → `BUILD FAILURE` (`cannot find symbol`), potem przywróceniem plików. GREEN natychmiast po przywróceniu: `15/15 PASS`. `SigningRoundTripTest` (`com.sepanexus.signature.internal`, Testcontainers PostgreSQL 18, brak Kafka, brak Springa — klasy konstruowane bezpośrednio jak `OrphanDlqTest`/`Pacs002CorrelationPersistenceTest`, fixed `ClockPort` lambda): round-trip sign→verify, exact-byte tamper detection, persistence shape (dokładnie jeden `OUTBOUND` wiersz, wszystkie kolumny), unknown/invalid key ref, wrong purpose, future/expired/revoked key, unsupported algorithm, invalid private material (+ potwierdzenie że nigdy nie trafia do komunikatu wyjątku), null artifact bytes, empty artifact bytes (dozwolone), null/blank signing key ref.

**Mutation-proof (Część 17) — wszystkie 3 poprawnie złapane, cofnięte:**
1. Podpisanie innych bajtów niż `artifactBytes` → dokładnie `signThenVerifyRoundTripsToVerified` FAIL → cofnięte.
2. Pominięcie insertu evidence → dokładnie `signPersistsExactlyOneOutboundMessageSignatureRow` FAIL → cofnięte.
3. Usunięcie warunku purpose (SIGN/BOTH) → dokładnie `verifyOnlyKeyIsRejectedForSigningAndPersistsNoRow` FAIL → cofnięte.
Po każdym cofnięciu: `15/15 PASS`, `git diff --check` czyste.

**Weryfikacja (Część 16, 18):** cross-schema SQL grep w `com.sepanexus.signature` → zero trafień. Targeted razem: `SigningRoundTripTest`+`SignatureVerificationTest`+`SignatureBeforeParseOrderingTest`+`KeyRegistryLookupTest`+`SignatureNoForeignRepoAccessTest`+`NonSignatureRoleCannotWriteSignatureTest` → `42/42 PASS`. Pełna regresja: `234/234 PASS` (było `219/219`), `BUILD SUCCESS`. Znany nieszkodliwy scheduler WARN "permission denied for schema iso" — 7x, zero nowych failures.

**Database review (skill `sepa-nexus-database-review`):** **PASS**, zero blocking findings. Non-blocking: (1) `signature.signature_keys.algo` bez DB-level CHECK ograniczającego do `Ed25519` (świadomy wybór, zgodny z konwencją Story 31.2); (2) konwencja `inline-pkcs8:` udokumentowana tylko w Javadoc, nie w komentarzu migracji.

**Planning:** `EPIC-31` Story 31.3A → `done` (evidence w epic file), epik `EPIC-31` → `done` (wszystkie 4 stories: 31.1/31.2/31.3A/31.4). `planning/README.md`'s EPIC-31/EPIC-43 rows zaktualizowane (EPIC-43 Story 43.2 świadomie NIE oznaczona READY — nadal blokowana przez nieukończoną 43.1). `planning/capability-graph.json` node `EPIC-31.31.3A` status `not-started`→`done`; `capability-graph.mmd` epic-level label `in-progress`→`done` + `S313A` node adnotacja. `planning/capabilities.yaml`'s `capabilities:` sekcja **celowo niezmieniona** — jest to udokumentowany, zamrożony zapis stanu SPRZED story-splitu (2026-07-16 backlog redesign), nie żywy indeks; źródłem prawdy jest `capability-graph.json`. `planning/BACKLOG-REDESIGN.md` **celowo niezmieniony** (zamrożony zapis historyczny). `story-inventory.json` przegenerowany (`--write`, 76/279 bez zmiany liczby). Wszystkie 6 walidatorów governance + `json.tool` na obu JSON-ach: PASS.

**Self-review:** `git diff --check` czyste; diff dotyka wyłącznie `com.sepanexus.signature*`, `SignedChannelIngestionPipeline`, `PaymentController` (komentarz), `planning/*` — nic poza zakresem tej story; żadna migracja; `egress` niezmieniony (nie istnieje); `outbound_artifact_id` nie fabrykowany (zawsze `NULL`); podpis obejmuje dokładne `artifactBytes`; evidence `OUTBOUND`; `covered_sha256` poprawny; purpose/validity/status egzekwowane; real Ed25519 (nic nie zmockowane); wszystkie mutacje cofnięte; żaden `[FREEZE]`/ADR/`AGENTS.md`/`CLAUDE.md` nietknięty; push NIE wykonany.

## Utknęliśmy na

Nic nie blokuje merytorycznie. Story 31.3A i cały `EPIC-31` w pełni ukończone i zweryfikowane.

## Plan na następny krok

1. **Push `main` — nadal FORBIDDEN w tej sesji, nadal nie wykonany.** Lokalny `main` teraz `8097b1a` (2 commity przed stanem sprzed tej sesji: `35ad40b`/`152808c` z main-consolidation session, plus ten sesji `8097b1a`). Zdalna gałąź `origin/feat/epic-27-4-orphan-queue` nadal nietknięta (jej usunięcie zależy od osobnej zgody użytkownika po udanym push `main` — patrz poprzedni HANDOFF).
2. **Rekomendowana kolejność `READY`** (z promptu tej sesji, Część 27): `EPIC-35/Story 35.1` (`SettlementStrategyResolver`) → `EPIC-43/Story 43.1` (`outbound_messages`+`SKIP LOCKED` dispatcher — teraz jedyny blocker dla `EPIC-43` Story 43.2, którego `31.3A`-zależność jest już spełniona) → `EPIC-32` (Ledger core) → `EPIC-25/Story 25.3B` (retry counter) → `EPIC-27/Story 27.5` (`[P1]`, operator read model). Nie zaczynaj `EPIC-43` Story 43.2 przed ukończeniem 43.1. Nie zaczynaj `EPIC-27` Story 27.3, dopóki brakujące capabilities z poprzedniej sesji nie zostaną dostarczone.
3. Rozbieżność `origin/main`/`main` z preflight (patrz "Zrobione") warta zweryfikowania na początku następnej sesji przez faktyczny `git push`/`git fetch` (gdy dozwolone) zamiast zakładania stanu z lokalnego cache.

## Pułapki, których nie wolno powtórzyć

- **Zanim zrefaktoryzujesz połączony port na dwa osobne, sprawdź źródłowy blueprint dosłownie** — w tym repo okazało się, że źródło JUŻ definiowało dwa porty (`SignatureVerificationPort`/`SignatureSigningPort`, linie 60-61 blueprintu), a połączony `SignaturePort` w kodzie był tylko tymczasowym uproszczeniem z wcześniejszej story. To nie było wynajdywanie nowej architektury — to było naprawienie kodu, żeby pasował do już zamrożonego źródła.
- **Żeby zachować uczciwy test-first RED, gdy implementacja jest już zaprojektowana/napisana w głowie**: fizycznie przenieś nowe pliki produkcyjne w tymczasowe miejsce (np. scratchpad), uruchom test, potwierdź `BUILD FAILURE`/`cannot find symbol`, potem przywróć pliki. Nie pisz testu PO cichu już mając gotową implementację i nie deklaruj RED bez faktycznego uruchomienia.
- **`signingKeyRef` (klucz-po-id) i `KeyRegistryPort.lookup` (klucz-po-participant+purpose+asOf) to CELOWO różne kształty lookupu** — nie próbuj re-używać `KeyRegistryPort` dla sign-path, potrzebny jest osobny internal-only `SigningKeyLookup` czytający po dokładnym `id`, bo signing-key-ref nazywa jeden konkretny klucz, nie "aktywny klucz dla tego uczestnika".
- **Nigdy nie zakładaj nieprzezroczystych testowych placeholderów (`"synthetic-lab-ref"`, `"ref-active"`) za "istniejącą konwencję prywatnego materiału"** — sprawdź, czy jakikolwiek PRODUKCYJNY kod faktycznie odczytuje/interpretuje tę wartość (`grep` po `privateMaterialRef`/`private_material_ref` poza rejestracją) zanim uznasz konwencję za zajętą/skonfliktowaną.
- Pułapki z poprzednich sesji (niezmienione, wciąż aktualne — patrz git historia tego pliku): broad epic-level `depends_on` może manufakturować cykle; `blocked` story może kryć już-`done` pracę; JPA write-behind pod przełączoną rolą; `act`+Testcontainers = zawsze `Could not find a valid Docker environment`; ArchUnit domyślnie skanuje test-classes (użyj `DO_NOT_INCLUDE_TESTS` dla scope produkcyjnego); `SECURITY DEFINER` wymaga jawnego `SET search_path`; współdzielony `@SpringBootTest` kontekst z żywym `@Scheduled` dispatcherem wygrywa wyścig z ręcznie skonstruowanym "broken broker" testem na tej samej bazie; nie ufaj, że twoja własna powłoka Bash mając dostęp zapisu do jakiejś ścieżki dowodzi, że INNY program (np. Codex CLI) ma ten sam dostęp.
