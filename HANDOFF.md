# HANDOFF

## Zadanie

Sesja `MAIN-CONSOLIDATION + SHARED-SKILL-DISCOVERY + EPIC-11-STORY-11.3` (2026-07-16, nowa, czysta sesja Claude Code CLI, branch startowy `feat/epic-27-4-orphan-queue` na `7e64447a540c64fdc0b3442a00f8e91323f4e660`, `EXPECTED_MAIN_COMMIT: 08610eec...` — oba potwierdzone, worktree czysty na starcie). Pięć celów: (1) weryfikacja dwóch ostatnich commitów Story 27.4, (2) czysty fast-forward do `main`, (3) usunięcie zbędnej gałęzi lokalnej/zdalnej, (4) uporządkowanie wspólnego wykrywania skills Claude Code/Codex, (5) readiness audit + implementacja `EPIC-11` Story 11.3. `PRODUCTION_CODE_CHANGES: ALLOWED ONLY FOR EPIC-11 Story 11.3`, `DATABASE_MIGRATIONS: FORBIDDEN`, `AGENT-INSTRUCTION-CHANGES: ALLOWED ONLY FOR SKILL DISCOVERY CONSOLIDATION` — wszystkie honorowane. Nie rozpoczęto: Story 11.2 implementation, EPIC-27 Story 27.3/27.5, EPIC-20 Story 20.3, GraphQL, `SECURITY DEFINER`, nowych katalogów `reference_data`, nowych migracji Flyway.

## Zrobione

**Preflight/branch comparison:** `main`=`08610ee`, `feature`=`7e64447`, merge-base=`main`, `main...feature`=`0 2` — dokładnie zgodne z oczekiwanym stanem. `HANDOFF.md` na starcie potwierdził, że poprzednia sesja (Story 27.4) zakończyła się kompletnie i zweryfikowanie.

**Feature verification przed integracją:** targeted (`Pacs002CorrelationPolicyTest` 9/9, `Pacs002CorrelationPersistenceTest` 7/7, `OrphanDlqTest` 5/5, `IsoOutboxTopicRoutingTest` 4/4 = 25/25) i pełna regresja (`214/214 PASS`, `BUILD SUCCESS`, zgodne z oczekiwanym baseline) — obie PASS. Wszystkie 6 governance validatorów PASS. Znany nieszkodliwy scheduler WARN "permission denied for schema iso" — 7x podczas pełnej regresji, ten sam udokumentowany wyścig `IsoOutboxDispatcher`/`SetLocalRoleSqlProofTest` na współdzielonym connection poolu, zero failures.

**Fast-forward:** `git switch main && git merge --ff-only feat/epic-27-4-orphan-queue` → czysty fast-forward, brak merge commita, HEAD = `7e64447a540c64fdc0b3442a00f8e91323f4e660` (dokładnie zgodny z oczekiwanym). Lokalna gałąź `feat/epic-27-4-orphan-queue` usunięta (`git branch -d`). Zdalna gałąź `origin/feat/epic-27-4-orphan-queue` **pozostaje** do usunięcia dopiero po udanym push `main` (Część 23 promptu sesji) — patrz "Plan na następny krok".

**Skills architecture (Część 7-9):** Sklasyfikowano `SKILL.md` (instrukcja + frontmatter `name`/`description`), `references/*.md` (szczegółowa wiedza), `scripts/*` (opcjonalne helpery, tylko `impeccable` je ma), `tools/agent-config/*.py|*.sh` (walidatory/generatory, NIE skille — jawnie wywoływane). Wszystkich 5 wymaganych database skills istnieje z poprawnym frontmatter i referencjami. **Decyzja: `KEEP EXISTING CODEX FALLBACK`** — `tools/codex/README.md` i `planning/epics/EPIC-00-repository-agent-foundation.md` dokumentują świadomą decyzję z sesji 2026-07-13: root `.agents/` jest systemowym read-only tmpfs w środowisku uruchomieniowym Codex CLI, stąd wybrany fallback `tools/codex/.agents/skills -> ../../../.claude/skills` + wymagane `codex --cd tools/codex`. Nie miałem możliwości uruchomić Codex CLI interaktywnie w tej sesji, żeby ponownie zweryfikować to ograniczenie z jego własnej perspektywy — próbny `mkdir .agents` w mojej powłoce Bash się powiódł, ale to NIE dowodzi, że proces Codex (potencjalnie inny sandbox/mount namespace) ma ten sam dostęp zapisu. Zgodnie z zasadą "nie zmieniaj struktury tylko dla estetyki bez weryfikacji", **nie wykonano migracji** — `.agents/skills` w root repo nie zostało utworzone, stary symlink/`README.md`/`CLAUDE.md`/`validate-database-skills.sh` niezmienione. Testowy katalog `.agents` utworzony podczas probingu usunięty (`rmdir`), brak pozostałości. Weryfikacja: symlink nadal resolves poprawnie, `validate-database-skills.sh` PASS, Claude Code widzi wszystkie 5 wymaganych skills (potwierdzone przez listę dostępnych skills tej sesji). Codex interaktywny test: **NOT RUN** (brak możliwości uruchomienia w tej sesji) — nie zadeklarowano fałszywego PASS.

**EPIC-11 Story 11.2 readiness audit (Część 10.1):** potwierdzone `grep`-em (nie założone), że `reference_data.status_catalog`/`reference_data.iso_reason_codes` nie istnieją w żadnej migracji Flyway — jedyne trafienie to komentarz w `V19__payment_status_history_and_events.sql`. Źródło (`sepa-nexus-message-flow-and-data-blueprint.md` §4.13, linie 891/894) ma pełny DDL, oznaczony `[MVP]` (nie `[NO-CODE]`) — więc to NIE jest sekwencjonowanie źródła jak `EPIC-12` Story 12.2. Prawdziwy problem: `EPIC-OWN-4`'s 4 sub-story (S1 participants, S2 iso_reason_codes/status_catalog+loaders, S3 calendars/scheme_profiles, S4 grant test) mają dziś w `planning/epics/EPIC-12-reference-data-ownership.md` tylko 2 stories (12.1=S3 `done`, 12.2=inne katalogi R-09 `blocked`) — **żadna story nie jest właścicielem S1 ani S2**. Story 11.2 zmieniona `not-started`→`blocked`/`[CAPABILITY-BLOCKED]` z dokładnym opisem tej luki planistycznej (nie stworzono nowych tabel/migracji/story w `EPIC-12` — poza zakresem tej sesji).

**EPIC-11 Story 11.3 (Część 10.2-16) — zaimplementowana i zweryfikowana:**
- Potwierdzono: `EPIC-09` Story 9.4 `done` (verify `OwnershipArchRulesTest` przechodzi), brak decision gate, brak brakującego schematu, brak potrzeby migracji.
- Audyt pakietów: tylko `com.sepanexus.modules.paymentlifecycle` istnieje produkcyjnie — `settlement`/`routing`/`egress` **nie istnieją nigdzie w kodzie** (ten sam finding co `epic10.IsoAdapterNoBusinessDecisionTest`'s własny komentarz). Cross-schema SQL grep w `payment-lifecycle` → zero trafień dla settlement/routing/egress (żadna referencja, nie tylko SQL).
- Nowy `backend/src/test/java/com/sepanexus/PaymentNoGodModuleTest.java`: reguła ArchUnit oparta na granicach pakietów (`.internal` = zabroniony pakiet implementacyjny modułu, root pakietu = dozwolony publiczny port — dokładnie konwencja już ustanowiona przez prawdziwy moduł `signature`), odporna na zmianę nazwy klasy.
- Non-vacuous proof: nowe fixture pod `backend/src/test/java/com/sepanexus/architecturefixtures/{paymentlifecycle/{forbidden,allowed}, settlement, routing, egress}` — 3 forbidden-dependency fixtures (settlement/routing/egress) + 1 allowed public-port fixture. 5 testów: produkcja PASS (dziś trywialnie, bo moduły nie istnieją), 3x reguła przeciw forbidden fixture → `EvaluationResult.hasViolation()==true` z komunikatem zawierającym nazwę zależnej klasy i zabronionego celu, 1x reguła przeciw allowed-port fixture → `hasViolation()==false`.
- Test-first: RED potwierdzony (`BUILD FAILURE`, "No tests matching pattern" — klasa nie istniała) przed utworzeniem plików → GREEN po implementacji (5/5 PASS).
- Mutation-proof 2x: (1) usunięcie `"..settlement.internal.."` z listy zabronionych → dokładnie `ruleCatchesForbiddenDependencyOnSettlementImplementation` poprawnie FAIL → cofnięte; (2) rozszerzenie zabronionego celu z `.internal` na cały pakiet `settlement` (usunięcie wyjątku publicznego portu) → dokładnie `ruleAllowsDependencyOnPublicSettlementPort` poprawnie FAIL → cofnięte. Oba razy 5/5 PASS po cofnięciu, `git diff --check` czyste.
- Sąsiednie testy architektoniczne (`IsoAdapterNoBusinessDecisionTest`, `SignatureNoForeignRepoAccessTest`, `OwnershipArchRulesTest`, `ModularityTest`) niezmienione, `13/13` razem z nowym testem PASS.
- Pełna regresja: `219/219 PASS` (było `214/214`), `BUILD SUCCESS`. Ten sam scheduler WARN 7x, zero nowych failures.

**Planning po implementacji:** `Story 11.3` → `done` (evidence w epic file), `Story 11.2` → `blocked`/`[CAPABILITY-BLOCKED]`, `EPIC-11` pozostaje `in-progress` (frontmatter niezmieniony — poprawne, bo 11.2 zablokowana). `planning/README.md`'s EPIC-11 row zaktualizowany. `planning/story-inventory.json` przegenerowany (`--write`, 76/279 bez zmiany liczby). `planning/BACKLOG-REDESIGN.md` **celowo niezmieniony** — to zamrożony zapis historyczny sesji 2026-07-16 (redesign), nie żywy indeks; jego "Ready queue" wpisy dla 11.2/11.3 są teraz nieaktualne względem obecnego stanu, ale zmiana tego dokumentu retroaktywnie zafałszowałaby dowód tamtej sesji. `capabilities.yaml`/`capability-graph.json`/`capability-graph.mmd` niezmienione — `EPIC-11` ma tam `granularity: epic-only` (nie był jednym z 12 deep-dived epików), a `.mmd`'s epic-level label "in-progress" pozostaje poprawny bez zmian. Wszystkie 6 governance validatorów + `json.tool` walidacja obu JSON-ów: PASS.

**Self-review (Część 19):** `git diff --check` czyste; brak zmian w `CLAUDE.md`/`.agents`/`tools/codex`/`tools/agent-config` (zgodne z decyzją KEEP EXISTING FALLBACK); brak nowych migracji; brak implementacji Story 11.2; fixture wyłącznie pod `backend/src/test/java`; ArchUnit produkcyjny test używa `DO_NOT_INCLUDE_TESTS`; brak cross-schema SQL; żaden `[FREEZE]`/ADR nietknięty; obie mutacje cofnięte, brak pozostałości ("mutation marker" grep czysty).

## Utknęliśmy na

Nic nie blokuje merytorycznie tę sesję. Story 11.3 w pełni ukończona i zweryfikowana. Story 11.2 poprawnie sklasyfikowana jako `[CAPABILITY-BLOCKED]` z jawnym `[OPEN-QUESTION]` (czy `EPIC-12` dostaje nową Story 12.3 dla S1+S2, czy S2 zostaje przeniesiona jako zależność 11.2) — do rozstrzygnięcia w przyszłej sesji, nie dziś. Skill discovery migration NIE wykonana — pozostaje `KEEP EXISTING CODEX FALLBACK` do czasu, aż ktoś faktycznie uruchomi Codex CLI z tego środowiska i potwierdzi/obali ograniczenie root `.agents/` read-only.

## Plan na następny krok

1. **Push `main` — próba odrzucona przez użytkownika w tej sesji** (`git push origin main`, permission denied). Lokalny `main` jest dziś 3 commity przed `origin/main` (`35ad40b` na szczycie, zawiera fast-forward Story 27.4 + nowy commit `test(architecture): guard payment lifecycle module boundaries`). Zdalna gałąź `origin/feat/epic-27-4-orphan-queue` **wciąż istnieje** — jej usunięcie było warunkowe na udany push, który nie nastąpił. Nie ponawiaj tego samego `git push` automatycznie w kolejnej sesji — zapytaj użytkownika, czy i kiedy chce wypchnąć `main` (mogą być powody spoza tej sesji, których nie znamy). Sprawdź `git branch -r`/`git log origin/main..main` na starcie zamiast zakładać stan.
2. Rekomendowana kolejność `READY` po Story 11.3 (z promptu tej sesji, nie z przestarzałego `BACKLOG-REDESIGN.md`): `EPIC-31/Story 31.3A` (standalone signing) → `EPIC-35/Story 35.1` (`SettlementStrategyResolver`) → `EPIC-43/Story 43.1` (outbound rail foundation) → `EPIC-32` (Ledger core) → `EPIC-25/Story 25.3B` (retry counter). Nie wybieraj automatycznie `EPIC-27 Story 27.5` mimo spełnionej zależności — jest `[P1]`, nie `[MVP]`.
3. Jeśli ktoś kiedyś faktycznie potwierdzi (przez realne uruchomienie Codex CLI z repo root), że root `.agents/` NIE jest już read-only w tym środowisku: wykonać migrację z Części 8 (`mkdir -p .agents && ln -s ../.claude/skills .agents/skills`, usunąć `tools/codex/.agents/skills`, zaktualizować `CLAUDE.md`/`tools/agent-config/validate-database-skills.sh`/`tools/codex/README.md`). Nie robić tego bez tej weryfikacji.
4. `EPIC-11` Story 11.2: rozstrzygnąć `[OPEN-QUESTION]` (nowa Story 12.3 w `EPIC-12` dla S1 `participants`/`participant_accounts` + S2 `iso_reason_codes`/`status_catalog`+loaders, czy inny podział) zanim ktokolwiek zacznie pisać migrację `status_catalog`/`iso_reason_codes`.

## Pułapki, których nie wolno powtórzyć

- **Nie ufaj, że twoja własna powłoka Bash mając dostęp zapisu do jakiejś ścieżki dowodzi, że INNY program (np. Codex CLI, potencjalnie inny sandbox/mount namespace) ma ten sam dostęp.** Testowy `mkdir /home/suso/debina/.agents` w tej sesji się powiódł mimo udokumentowanego (z realnej inwestygacji sesji 2026-07-13) twierdzenia, że root `.agents/` jest read-only w środowisku Codex — nie potraktowałem tego jako obalenia poprzedniego ustalenia, bo nie mogłem uruchomić samego Codex CLI, żeby to zweryfikować z jego perspektywy. Nadpisanie ustalonej, przetestowanej struktury na podstawie niepełnego dowodu byłoby ryzykowne dla cross-tool skill discovery.
- **`ArchRule.evaluate(JavaClasses)` (nie `.check()`) zwraca `EvaluationResult` z `.hasViolation()`/`.getFailureReport().getDetails()`** — to jest droga do asercji non-vacuous proof bez polegania na przechwytywaniu `AssertionError` z `.check()`. `archunit` w tym repo to `1.4.2` (transitywne przez `spring-modulith-starter-test`).
- **Fixture pod `architecturefixtures.*` muszą mieć dokładnie te same nazwy segmentów pakietu co realna reguła sprawdza** (`paymentlifecycle`, `settlement.internal`, itd.) — inaczej `resideInAPackage("..paymentlifecycle..")`/`resideInAnyPackage("..settlement.internal..")` nie złapie fixture w ogóle, i "non-vacuous proof" byłby cichym no-op.
- **`com.sepanexus.signature`/`.internal` to już istniejący, prawdziwy precedens** dla konwencji "root pakietu modułu = publiczny port, `.internal` = zabroniona implementacja" — użyj go zamiast wymyślać nową konwencję dla settlement/routing/egress, które jeszcze nie istnieją.
- Pułapki z poprzednich sesji (niezmienione, wciąż aktualne — patrz git historia tego pliku): broad epic-level `depends_on` może manufakturować cykle; epik "not done" może kryć gotową sub-capability; `blocked` story może kryć już-`done` pracę; JPA write-behind pod przełączoną rolą; `act`+Testcontainers = zawsze `Could not find a valid Docker environment`; ArchUnit domyślnie skanuje test-classes (użyj `DO_NOT_INCLUDE_TESTS` dla scope produkcyjnego); `SECURITY DEFINER` wymaga jawnego `SET search_path`; współdzielony `@SpringBootTest` kontekst z żywym `@Scheduled` dispatcherem wygrywa wyścig z ręcznie skonstruowanym "broken broker" testem na tej samej bazie — użyj dedykowanego izolowanego kontenera bez kontekstu Springa dla takich testów.
