# Progress — PILOT-SPEC-KIT-CURSOR-ONLY

## Status

`READY_FOR_CLOSEOUT` → independent review `PASS_WITH_FINDINGS`

## Disposable root

`/tmp/debina-spec-kit-cursor-pilot/`  
(Clean init path: `clean-project/` because existing empty `clean/` blocked init without `--force`)

## SPEC_KIT_TAG

`v0.14.3` (`ec45316a…` tag; CLI 0.14.3)

## Completed stages

1. Preflight + bootstrap (STANDARD / IMPLEMENTING / APPROVED)
2. Release pin + uvx CLI smoke
3. Experiment A clean Cursor inventory
4. Experiment B whole-directory symlink → `WHOLE_DIRECTORY_SYMLINK_UNSAFE`
5. Experiment C per-skill bridges → upgrade/uninstall safe for sentinel
6. Lifecycle: upgrade idempotence, modified protection, uninstall, rollback conflict
7. Constitution + active feature pointer tests
8. Cursor runtime discovery attempted → auth required (manual reopen)
9. Workflow static/CLI partial (not full agent runtime)
10. Main-repo integrity documented
11. Adoption: `ADOPT_CURSOR_WITH_LAYOUT_CHANGE`

## Force usage

None.

## Persistent install

None (`uvx` only).

## Debina Spec Kit install

None.
