# Completion claim gate

Before a completion/commit/handoff claim: run fresh targeted and required regression commands; inspect exit codes and counts; inspect logs; restore generated artifacts; run planning and skill validators; run `git diff --check`; inspect status; then classify honestly.
