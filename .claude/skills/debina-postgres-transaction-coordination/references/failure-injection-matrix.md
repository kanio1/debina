# Failure-injection matrix

Prove success and a rollback before/after each module command, inside-function failure, same-command replay, different-command conflict, identical/conflicting concurrency, exact grant matrix, direct-DML rejection, hostile search_path, SECURITY INVOKER/PUBLIC/grant mutations, pool/tenant isolation, second-connection detection, fresh/upgrade migration, and deadlock freedom. Revert every mutation immediately.
