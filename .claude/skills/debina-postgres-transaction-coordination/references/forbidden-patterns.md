# Forbidden patterns

No unapproved `SET LOCAL ROLE`, coordinator table grants, XA/JTA, generic SQL executor, omnipotent function, user-writable search_path, dynamic SQL without approval, or claim that sequenced commits are ACID. Do not invent saga, compensation, finality, or reverse semantics.
