# Concurrency proof matrix

Use deterministic account lock order. Prove concurrent reserve cannot exceed available liquidity; concurrent post/release yields one legal terminal effect; duplicate replay preserves the first result; conflicting terminal command fails; no deadlock or duplicate journal effect occurs.
