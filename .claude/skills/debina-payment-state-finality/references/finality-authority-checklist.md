# Finality authority checklist

Require settlement ownership, profile snapshot, versioned rule, real trigger, source identifier, occurrence time, evidence hash, one record, replay behavior, conflict failure and projection idempotency. `ACSC`, `DELIVERED`, receipt and FSM terminality are not authority.
