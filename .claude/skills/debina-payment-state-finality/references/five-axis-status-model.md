# Five-axis status model

Business lifecycle, ISO/message status, settlement finality, transport/delivery status, and receipt/reconciliation status are independent. Each event changes only its owned axis unless an approved rule explicitly authorizes more.
