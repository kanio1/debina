# Use-Case to Backlog Map

The YAML map is the Phase E migration input. C2 deliberately maps only representative single-SCT, signed ingestion, routing, correlation and maker-checker groups; it does not alter their story metadata or pretend legacy readiness. File, clearing, settlement, R-transaction, egress and reconciliation mappings identify missing/planned capability rather than creating implementation work.
