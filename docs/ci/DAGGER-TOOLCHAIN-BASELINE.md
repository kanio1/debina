# Dagger toolchain baseline

## Checkpoint recorded 2026-07-22

| Item | Observed | Required for Phase D |
|---|---|---|
| Dagger CLI | absent (`command -v dagger` failed) | stable `v0.21.4`, with matching Engine and Go SDK generated by that CLI |
| Dagger Engine | not probeable without CLI | `registry.dagger.io/engine:v0.21.4`; module `engineVersion` must match |
| Go | absent (`command -v go` failed) | a current Go toolchain capable of compiling the CLI-generated Go module; record exact version after initialization |
| Podman | 5.8.4, linux/amd64, rootless, cgroup v2 | Dagger's official Podman guidance requires rootful execution and warns about a low engine PID limit |
| Host | Fedora 44, kernel `7.1.4-200.fc44.x86_64`, amd64 | retain current architecture/kernel evidence |
| PID limit | cgroup v2 detected; host limit was not readable through this session | record the Engine container PID limit during the post-install probe |

The user-owned `DOCKER_HOST` value was detected but intentionally redacted. No secret-valued environment variable is recorded.

## Required user actions before retry

1. Install the pinned Dagger CLI using the [official Dagger installation guidance](https://docs.dagger.io/getting-started/installation/):

   ```bash
   curl -fsSL https://dl.dagger.io/dagger/install.sh | DAGGER_VERSION=0.21.4 BIN_DIR="$HOME/.local/bin" sh
   ```

   This was not run by Phase D: it downloads and installs software, and the request forbids silent installation.
2. Install a Go toolchain using the Fedora/Go distribution method owned by the workstation user.
3. Satisfy Dagger's [official Podman prerequisite](https://docs.dagger.io/reference/container-runtimes/podman): rootful Podman and a safe Engine PID limit. This is a security-relevant host change and was not made here.
4. Retry with `dagger version && dagger core version && dagger check --help` from this repository. The first Dagger operation must be the minimal deterministic engine probe, before repository content is loaded.

The official Dagger documentation describes `// +check`, unfiltered `dagger check`, workspace inputs, and committed generated Go bindings; those details will be verified against the installed `v0.21.4` CLI before creating files.
