# Use-case Traceability Validation

Phase C1 validator: error for story without required future slice/quality-use-case trace; error for a slice referencing a missing use case; error for business rule without source registry key/tag; warn `legacy-untraced` only until controlled Phase E migration. Diagnostics include stable ID, location and remediation.
