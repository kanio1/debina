# C4 Containers

The browser uses the Next.js frontend/BFF. It calls a Spring Modulith backend; the backend uses PostgreSQL, Kafka and Keycloak. A simulated CSM is external to the system boundary. Local Dagger Go SDK automation is planned infrastructure, invoked by developers or Codex—not a remote pipeline.
